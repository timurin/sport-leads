[project]
name = "sport-leads"
version = "0.1.0"
description = "Platform for collecting sports events and organizer contacts"
readme = "README.md"
requires-python = ">=3.13"

authors = [
    { name = "Тимур" }
]

dependencies = []