# Sport-Lead Roadmap

**Platform version:** `v0.3.0-demo`
**Roadmap code:** `SL-ROADMAP-v0.3.0`
**Structure code:** `SL-ERP-CHECK-v0.3.0`
**Предыдущий код roadmap:** `SL-ROADMAP-v1`
**Статус документа:** исторический; актуальный план — [SL-ROADMAP-v0.6.1](SL-ROADMAP-v0.6.1.md)

Архивная копия. Канонический roadmap: `docs/roadmap/roadmap.md`; канонические архитектурные документы: `docs/architecture/project-structure.md` и `docs/architecture/erp-check.md`.

## Сквозная производственная цепочка

Заказ покупателя → позиции заказа → номенклатура → модель/артикул → размеры и характеристики → производственная партия → утверждённая спецификация партии → печать/раскрой/пошив → фактический расход и выпуск → передача данных и документов в 1С.

---

## Легенда

* `[x]` — выполнено и подтверждено
* `[~]` — частично реализовано или работает на демонстрационных данных
* `[ ]` — не реализовано
* `[!]` — требует дополнительной проверки или стабилизации

---

# Текущее состояние платформы

## Подтверждено

* `[x]` создан backend на FastAPI;
* `[x]` используется SQLAlchemy;
* `[x]` подключен Alembic;
* `[x]` PostgreSQL запускается через Docker Compose;
* `[x]` создан frontend на Next.js;
* `[x]` frontend проходит lint и production build;
* `[x]` создана общая ERP-навигация;
* `[x]` реализован интерфейс раздела продаж;
* `[x]` реализованы Kanban-доски лидов и сделок;
* `[x]` реализована таблица клиентов;
* `[x]` реализованы frontend-формы создания и редактирования сущностей;
* `[x]` реализован каталог материалов;
* `[x]` для материалов существуют backend API и модель базы данных;
* `[x]` добавлен dashboard продаж;
* `[x]` реализованы фильтры dashboard;
* `[x]` реализованы KPI продаж;
* `[x]` реализована воронка продаж;
* `[x]` реализована динамика продаж;
* `[x]` реализована аналитика источников лидов;
* `[x]` реализованы сводки по заказам и задачам;
* `[x]` реализована лента последних событий;
* `[x]` исправлен drag-and-drop в Kanban;
* `[x]` создан checkpoint `v0.3.0-demo`;
* `[x]` создан recovery guide;
* `[x]` существует комплексная проверка `scripts/check_project.py`.

## Основные ограничения текущей версии

* `[~]` интерфейсы CRM используют демонстрационные данные;
* `[~]` изменения Kanban не сохраняются после перезагрузки;
* `[~]` лиды, сделки, клиенты и заказы существуют преимущественно как frontend-контур;
* `[ ]` нет полной backend-интеграции CRM;
* `[ ]` нет авторизации;
* `[ ]` нет ролей и прав доступа;
* `[ ]` нет realtime-обновлений;
* `[ ]` нет внешних CRM-интеграций;
* `[ ]` нет полного производственного контура;
* `[ ]` нет полноценного складского документооборота;
* `[ ]` нет закупок;
* `[ ]` нет логистического контура;
* `[ ]` нет фактического финансового учета и себестоимости.

---

# Этап 0. Стабилизация платформы

## Цель

Получить устойчивую техническую основу, на которой можно безопасно развивать CRM, производство, склад и финансы.

## Выполнено

* `[x]` создана структура backend;
* `[x]` создана структура frontend;
* `[x]` подключен FastAPI;
* `[x]` подключен SQLAlchemy;
* `[x]` подключен Alembic;
* `[x]` настроен PostgreSQL;
* `[x]` настроен Docker Compose;
* `[x]` настроены переменные окружения;
* `[x]` создан Git-репозиторий;
* `[x]` используется основная ветка `main`;
* `[x]` frontend проходит `npm run lint`;
* `[x]` frontend проходит `npm run build`;
* `[x]` FastAPI и OpenAPI входят в общую проверку проекта;
* `[x]` SQLAlchemy-модели входят в общую проверку;
* `[x]` Alembic входит в общую проверку;
* `[x]` Docker Compose проходит проверку конфигурации;
* `[x]` создан `scripts/check_project.py`;
* `[x]` создан checkpoint `v0.3.0-demo`;
* `[x]` создан защищенный recovery-процесс.

## Осталось

* `[ ]` добавить полноценный pytest-контур;
* `[ ]` добавить тестовую базу данных;
* `[ ]` добавить Ruff;
* `[ ]` добавить отдельную проверку TypeScript;
* `[ ]` добавить frontend unit-тесты;
* `[ ]` добавить backend integration-тесты;
* `[ ]` настроить CI/CD;
* `[ ]` добавить централизованное логирование;
* `[ ]` добавить мониторинг ошибок;
* `[ ]` формализовать архитектурные правила;
* `[ ]` заполнить корневой README;
* `[ ]` создать единый индекс документации.

## Контрольная точка

Проект разворачивается из чистого окружения, backend и frontend запускаются стандартными командами, миграции применяются автоматически, а основные сценарии проверяются тестами и CI.

---

# Этап 1. Backend-интеграция CRM

## Цель

Перевести существующий демонстрационный интерфейс продаж на реальные данные PostgreSQL.

## Уже реализовано

* `[x]` ERP-навигация раздела продаж;
* `[x]` Kanban лидов;
* `[x]` Kanban сделок;
* `[x]` таблица клиентов;
* `[x]` frontend-формы создания и редактирования;
* `[x]` dashboard продаж;
* `[x]` фильтрация dashboard;
* `[x]` аналитические frontend-компоненты;
* `[x]` исправлен drag-and-drop.

## Необходимо реализовать

### Общая CRM-модель

* `[ ]` определить единую модель `Lead`;
* `[ ]` определить единую модель `Deal`;
* `[ ]` определить единую модель `Client`;
* `[~]` определить единую модель `Contact` — итерации `v0.6.1-lead-contact-persistence` и `v0.6.1-lead-contact-frontend`: добавлены постоянная `LeadContact`, отдельная таблица, атомарный primary, API и подключение contact-форм числовой detail-страницы без demo fallback; подтверждения: `backend/app/models/sales.py`, `backend/app/services/lead_contacts.py`, `backend/tests/test_lead_contacts.py` (3 теста), `frontend/app/(workspace)/sales/leads/[leadId]/lead-contact-actions.ts`, `frontend/lib/sales/lead-contact-api.test.mjs` (2 теста), полный backend pytest (24 теста) и frontend tests (24 теста); ограничение: контакты `Client`, demo-лиды `lead-1`/`lead-2` и поле messenger не имеют общего persistence;
* `[ ]` определить единую модель `CustomerOrder`;
* `[ ]` определить связи между сущностями;
* `[ ]` создать миграции;
* `[ ]` создать Pydantic-схемы;
* `[ ]` создать репозитории;
* `[ ]` создать сервисный слой;
* `[ ]` создать API CRUD.

### Лиды

* `[~]` интерфейс списка и Kanban;
* `[~]` карточка лида — числовая detail-страница использует общие backend-стадии, сохраняет системные/custom переходы и запускает постоянные convert/reject через неизменяемые финальные действия; задачи, заметки и сообщения ещё частичны;
* `[~]` загрузка лидов из backend — итерация `v0.6.1-lead-list-backend-load`: `/sales/leads` загружает список через server-side `GET /leads?limit=500`, строит Kanban из API-ответа и при ошибке показывает явное сообщение без скрытой demo-подмены; диагностическая доработка `v0.6.1-lead-list-dev-seed`: добавлена явная идемпотентная development seed-команда `SPORT_LEADS_ALLOW_DEV_SEED=1 .\.venv\Scripts\python.exe scripts\seed_sales_dev.py` для 5 backend-лидов с числовыми id, контактами и коммерческими данными без `lead-1`/`lead-2` fallback; подтверждения: `frontend/app/(workspace)/sales/leads/page.tsx`, `frontend/lib/sales/lead-list-api.ts`, `frontend/lib/sales/lead-list-mapping.ts`, `frontend/components/sales/lead-workspace.tsx`, `frontend/lib/sales/lead-list-api.test.mjs` (2 теста), `scripts/seed_sales_dev.py`, `backend/tests/test_sales_dev_seed.py`; ограничение: пункт не повышен до `[x]` до ручной проверки списка с реальными backend-записями, создание лида, сохранение перемещений Kanban, convert/reject из списка, demo detail-лиды, задачи, заметки и timeline остаются local/demo;
* `[x]` создание лида через API — итерация `v0.6.1-lead-creation`: добавлен `POST /leads`, который через service-слой атомарно создаёт существующие `Lead`, основной `LeadContact` и событие `lead_created`; рабочая frontend-форма списка сохраняет лид в PostgreSQL без demo fallback; подтверждения: `backend/app/services/lead_creation.py`, `backend/app/api/leads.py`, `backend/tests/test_lead_creation.py`, `frontend/components/sales/lead-create-dialog.tsx`, `frontend/app/(workspace)/sales/leads/lead-create-actions.ts`, `frontend/lib/sales/lead-creation.test.mjs`; проверки: backend pytest 32, frontend tests 41, TypeScript, ESLint, production build, project check 9/9, OpenAPI и Alembic check; ограничение: архивирование не входит в эту подзадачу;
* `[~]` редактирование лида через API — итерации `v0.6.1-lead-commercial-frontend`, `v0.6.1-lead-customer-profile-persistence` и `v0.6.1-lead-card-save-fix`: форма числовой detail-страницы загружает и сохраняет через существующий PATCH core-профиль клиента, контакты, стадии, source и полный коммерческий блок карточки, включая расширенные поля, `delivery_city`, даты, бюджет/суммы/проценты и priority; подтверждения: `backend/app/models/sales.py`, `backend/app/schemas/sales.py`, `backend/app/api/leads.py`, `backend/alembic/versions/c12f0f2d0f4b_add_lead_customer_profile.py`, `backend/alembic/versions/f2a7c4d8e901_add_lead_card_commercial_fields.py`, `frontend/app/(workspace)/sales/leads/[leadId]/lead-commercial-actions.ts`, `frontend/app/(workspace)/sales/leads/[leadId]/lead-customer-actions.ts`, `frontend/lib/sales/lead-commercial-api.test.mjs` (3 теста), `frontend/lib/sales/lead-customer-api.test.mjs` (3 теста), `backend/tests/test_lead_conversion.py` (customer profile, commercial PATCH и responsible PATCH), полный backend pytest (37 тестов), frontend tests (50 тестов), typecheck, lint, production build, Alembic upgrade/check и project check 9/9 с `PYTHONUTF8=1`; ограничение: demo-лиды, полноценный frontend-список backend-пользователей для ответственного, messenger, задачи, заметки и timeline остаются local/demo или отдельными подзадачами;
* `[ ]` удаление или архивирование;
* `[x]` сохранение позиции в Kanban — итерации `v0.6.1-lead-custom-stage-persistence` и `v0.6.1-lead-detail-custom-stages`: системные и пользовательские стадии хранятся в `lead_stages`, числовой API-лид сохраняет стабильный ID стадии в `Lead.status`, а Kanban и detail-карточка используют общий backend-список и общий PATCH без local successful fallback; подтверждения: `backend/app/services/lead_stages.py`, `backend/app/api/lead_stages.py`, `backend/tests/test_lead_stages.py`, `frontend/lib/sales/lead-stage-api.ts`, `frontend/components/sales/lead-workspace.tsx`, `frontend/components/sales/lead-header.tsx`, `frontend/lib/sales/lead-detail-stage.test.mjs`; проверки: backend pytest 34, frontend tests 47, TypeScript, lint, build, Alembic check и project check 9/9; ограничение: интерактивный browser-smoke недоступен;
* `[~]` история изменения статусов — обычные перемещения и атомарные переносы при отключении стадии создают `LeadEvent(lead_status_changed)`, но полный аудит конфигурации стадий и local/demo-активности ещё отсутствуют;
* `[ ]` причины отказа;
* `[ ]` защита от дублей;
* `[~]` поиск по телефону и email — итерация `v0.6.1-lead-duplicate-candidates`: добавлен read-only backend endpoint `GET /leads/duplicate-candidates` для поиска кандидатов по email и нормализованному телефону в `Lead`/`LeadContact`; подтверждения: `backend/app/services/lead_duplicates.py`, `backend/app/api/leads.py`, `backend/tests/test_lead_duplicates.py`; проверки: backend pytest 36 и project check 9/9 с `PYTHONUTF8=1`; ограничения: создание лида не блокируется, UI-предупреждение и слияние дублей не реализованы;
* `[ ]` конвертация в клиента;
* `[ ]` конвертация в сделку;
* `[ ]` конвертация в заказ.

### Сделки

* `[~]` интерфейс Kanban;
* `[ ]` backend-модель сделки;
* `[ ]` API сделок;
* `[ ]` сохранение статусов;
* `[ ]` сумма сделки;
* `[ ]` вероятность;
* `[ ]` плановая дата закрытия;
* `[ ]` связь с лидом;
* `[ ]` связь с клиентом;
* `[ ]` связь с заказом;
* `[ ]` причины проигрыша.

### Клиенты

* `[~]` интерфейс таблицы;
* `[~]` интерфейс инспектора клиента;
* `[ ]` backend-модель клиента;
* `[ ]` юридические реквизиты;
* `[ ]` контактные лица;
* `[ ]` адреса;
* `[ ]` история заказов;
* `[ ]` история коммуникаций;
* `[ ]` задолженность;
* `[ ]` договоры;
* `[ ]` персональные условия.

### Задачи и активности

* `[~]` демонстрационная сводка задач;
* `[~]` демонстрационная лента событий;
* `[ ]` backend-модель задачи;
* `[ ]` создание задач;
* `[ ]` ответственный;
* `[ ]` постановщик;
* `[ ]` дедлайн;
* `[ ]` приоритет;
* `[ ]` статус;
* `[ ]` комментарии;
* `[ ]` привязка к CRM-сущности;
* `[ ]` реальные активности;
* `[ ]` история изменений.

### Файлы и комментарии

* `[ ]` файловое хранилище;
* `[ ]` загрузка вложений;
* `[ ]` версии файлов;
* `[ ]` комментарии;
* `[ ]` упоминания сотрудников;
* `[ ]` журнал событий.

## Контрольная точка

Менеджер создает лид, редактирует его, меняет статус в Kanban, перезагружает страницу и видит сохраненные данные. Лид можно преобразовать в клиента, сделку и заказ без повторного ввода информации.

---

# Этап 2. Заказы покупателей

## Цель

Создать основной коммерческий документ, который связывает продажи с производством.

## Текущее состояние

* `[x]` карточка заказа покупателя доступна по `/sales/orders/[orderId]` и загружает сохранённый `SalesOrder` из backend;
* `[x]` организация, статусы и статусная история заказа имеют постоянный backend-контур;
* `[x]` коммерческие позиции постоянно хранят snapshot-наименование, единицу, количество, цену и сумму строки;
* `[x]` итоговая сумма заказа пересчитывается из позиций через `Decimal/Numeric`;
* `[~]` заказы отображаются на dashboard, но документы, оплаты и production workflow отсутствуют.

## Необходимо реализовать

### Карточка заказа

* `[x]` номер и дата — итерация `v0.7.0-sales-order-foundation`: read-only header на основе `GET /orders/{order_id}`, подтверждён `backend/tests/test_lead_conversion.py` и `frontend/lib/sales/order-details.test.mjs`;
* `[x]` организация — итерация `v0.7.2-sales-order-organization-foundation`: добавлены persistent `Organization`, nullable связь `SalesOrder.organization_id`, создание/переиспользование организации при конвертации, `GET /organizations`, `PATCH /orders/{order_id}/organization` и отображение в карточке; ограничения: настройки организаций пока demo-only, старые заказы без организации требуют назначения;
* `[x]` клиент — итерация `v0.7.0-sales-order-foundation`: detail API возвращает имя клиента, карточка показывает его без demo fallback;
* `[ ]` контактное лицо;
* `[x]` ответственный менеджер — итерация `v0.7.0-sales-order-foundation`: detail API возвращает имя `SalesUser`, nullable значение отображается явно;
* `[ ]` договор;
* `[ ]` срок готовности;
* `[ ]` способ доставки;
* `[ ]` адрес доставки;
* `[ ]` условия оплаты;
* `[x]` статус заказа — итерации `v0.7.0-sales-order-kanban-status-persistence` и `v0.7.1-sales-order-status-workflow-history`: Kanban сохраняет `SalesOrder.status`, backend проверяет переходы и пишет статусную историю в `LeadEvent`; reload восстанавливает статус и историю. Подтверждения: `backend/app/services/sales_order_status.py`, `backend/app/api/orders.py`, `backend/alembic/versions/a91d6e3f4b20_add_order_status_changed_event.py`, `backend/tests/test_lead_conversion.py`, `frontend/lib/sales/order-details.ts`, `frontend/components/sales/sales-order-page.tsx`; тесты: backend pytest 42, frontend tests 52, TypeScript, lint, build, project check 9/9;
* `[ ]` статус оплаты;
* `[ ]` комментарии;
* `[ ]` файлы.

### Товарные позиции

* `[x]` минимальные товарные позиции — итерация `v0.7.3-sales-order-items` и patch `v0.7.3-remove-material-from-order-items`: `SalesOrderItem` хранит только `snapshot_name`, `unit`, `quantity`, `unit_price`, `line_amount`; CRUD API и карточка заказа пересчитывают `SalesOrder.amount`. Material, Product-каталог и production-документы не являются источниками номенклатуры позиции;

Позиция заказа остаётся коммерческой строкой. Связь с будущей номенклатурой будет добавлена отдельной задачей; материалы и спецификации в этап 2 не входят.

* `[ ]` наименование;
* `[ ]` модель;
* `[ ]` артикул;
* `[ ]` количество;
* `[ ]` размерный ряд;
* `[ ]` персонализация;
* `[ ]` цвет;
* `[ ]` цена;
* `[ ]` скидка;
* `[ ]` сумма;
* `[ ]` НДС;
* `[ ]` макет;
* `[ ]` лекало;
* `[ ]` спецификация;
* `[ ]` техкарта.

### Расчеты

* `[ ]` автоматический расчет суммы;
* `[ ]` скидки по позиции;
* `[ ]` скидка по заказу;
* `[ ]` НДС;
* `[ ]` доставка;
* `[ ]` предоплата;
* `[ ]` остаток к оплате;
* `[ ]` плановая себестоимость;
* `[ ]` маржа.

### Документы

* `[ ]` коммерческое предложение;
* `[ ]` счет;
* `[ ]` спецификация;
* `[ ]` заказ покупателя;
* `[ ]` печатная форма;
* `[ ]` PDF;
* `[ ]` история версий.

## Контрольная точка

Заказ создается из лида или сделки, содержит все товарные позиции, размеры, цены, файлы и сроки и готов к передаче в производственный контур.

---

# Этап 3. Продукция, модели, лекала и техкарты

## Цель

Связать продаваемую продукцию с технологией производства.

Целевая последовательность: Номенклатура → модели и артикулы → размеры и характеристики → лекала → операционные узлы → нормативная технология.

## Текущее состояние

* `[~]` существуют интерфейсы ERP-каталогов;
* `[x]` реализован каталог материалов;
* `[x]` существует backend API материалов;
* `[x]` существует модель базы данных материалов;
* `[~]` структура моделей и лекал ранее спроектирована;
* `[ ]` полный рабочий контур моделей и техкарт не подтвержден.

## Необходимо реализовать

### Номенклатура

* `[ ]` готовое изделие;
* `[ ]` модель;
* `[ ]` связь готового изделия с моделью/артикулом;
* `[ ]` полуфабрикат;
* `[ ]` услуга;
* `[ ]` комплект;
* `[ ]` категории;
* `[ ]` единицы измерения;
* `[ ]` характеристики;
* `[ ]` изображения.

### Модели

* `[ ]` карточка модели;
* `[ ]` артикул;
* `[ ]` категория;
* `[ ]` вид изделия;
* `[ ]` размерный ряд;
* `[ ]` фотографии;
* `[ ]` активность;
* `[ ]` версии;
* `[ ]` архив;
* `[ ]` стоимость пошива;
* `[ ]` связанные лекала;
* `[ ]` связанные спецификации.

Материалы являются производственными ресурсами для спецификаций, печати, раскроя, пошива и планового/фактического списания; они не являются номенклатурой заказа.

### Лекала

* `[ ]` карточка лекала;
* `[ ]` связь с моделью;
* `[ ]` размеры;
* `[ ]` версия;
* `[ ]` файлы;
* `[ ]` конструктор;
* `[ ]` дата утверждения;
* `[ ]` статус;
* `[ ]` история изменений.

### Спецификации

* `[ ]` список операций;
* `[ ]` нормы материалов;
* `[ ]` время операции;
* `[ ]` стоимость операции;
* `[ ]` производственный участок;
* `[ ]` инструкции;
* `[ ]` контрольные точки;
* `[ ]` версия спецификации.

### Техкарты заказа

* `[ ]` создание из модели;
* `[ ]` копирование спецификации;
* `[ ]` индивидуальные изменения;
* `[ ]` фиксация версии;
* `[ ]` связь с товарной позицией;
* `[ ]` макеты;
* `[ ]` технические файлы;
* `[ ]` маршрут производства.

## Контрольная точка

Для каждой товарной позиции заказа можно выбрать модель, лекало и спецификацию и сформировать технологическую карту заказа.

---

# Этап 4. Дизайн и согласование

## Цель

Организовать работу дизайнеров и исключить запуск производства по несогласованным файлам.

## Необходимо реализовать

* `[ ]` задания дизайнеру;
* `[ ]` очередь заданий;
* `[ ]` приоритет;
* `[ ]` дедлайн;
* `[ ]` назначение дизайнера;
* `[ ]` техническое задание;
* `[ ]` файлы клиента;
* `[ ]` версии макетов;
* `[ ]` внутреннее согласование;
* `[ ]` отправка клиенту;
* `[ ]` комментарии клиента;
* `[ ]` доработка;
* `[ ]` фиксация утвержденной версии;
* `[ ]` публичная страница согласования;
* `[ ]` электронная фиксация согласования;
* `[ ]` запрет запуска печати без утверждения.

## Контрольная точка

Производство получает только утвержденную версию макета, а все предыдущие версии и комментарии сохраняются в истории.

---

# Этап 5. Производственный контур MVP

## Цель

Отслеживать прохождение заказа по производственным участкам.

Целевая последовательность: производственные партии → спецификации партий → утверждение версии → печать → раскрой → пошив → ОТК → фактический расход → выпуск продукции. Спецификация является планом и формируется до запуска партии; производственный факт фиксируется отдельно.

## Необходимо реализовать

### Заказ на производство

* `[ ]` создание из заказа покупателя;
* `[ ]` список изделий;
* `[ ]` размеры;
* `[ ]` количество;
* `[ ]` срок;
* `[ ]` приоритет;
* `[ ]` техкарта;
* `[ ]` плановая себестоимость;
* `[ ]` общий статус.

### Производственные задания

* `[ ]` дизайн;
* `[ ]` подготовка файлов;
* `[ ]` печать;
* `[ ]` крой;
* `[ ]` комплектация;
* `[ ]` пошив;
* `[ ]` контроль качества;
* `[ ]` упаковка;
* `[ ]` передача на склад.

### Для каждого задания

* `[ ]` участок;
* `[ ]` ответственный;
* `[ ]` исполнитель;
* `[ ]` плановое начало;
* `[ ]` плановое завершение;
* `[ ]` фактическое начало;
* `[ ]` фактическое завершение;
* `[ ]` количество в работе;
* `[ ]` выполненное количество;
* `[ ]` брак;
* `[ ]` переделка;
* `[ ]` комментарии;
* `[ ]` файлы;
* `[ ]` просрочка.

### Производственный dashboard

* `[ ]` заказы в работе;
* `[ ]` загрузка участков;
* `[ ]` просроченные задания;
* `[ ]` узкие места;
* `[ ]` готовность заказа;
* `[ ]` прогноз завершения.

## Контрольная точка

Руководитель производства видит, где находится каждый заказ, кто выполняет текущий этап, сколько изделий готово и когда заказ будет завершен.

---

# Этап 6. Складской контур

## Цель

Получить достоверные остатки материалов, полуфабрикатов и готовой продукции.

## Текущее состояние

* `[x]` существует каталог материалов;
* `[x]` существует API материалов;
* `[x]` существует модель материалов;
* `[ ]` складские документы не реализованы.

## Необходимо реализовать

### Склады

* `[ ]` склад материалов;
* `[ ]` производственный склад;
* `[ ]` склад готовой продукции;
* `[ ]` зоны хранения;
* `[ ]` ответственные лица.

### Операции

* `[ ]` поступление;
* `[ ]` перемещение;
* `[ ]` резервирование;
* `[ ]` передача в производство;
* `[ ]` возврат из производства;
* `[ ]` списание;
* `[ ]` выпуск готовой продукции;
* `[ ]` прием готовой продукции;
* `[ ]` комплектация;
* `[ ]` отгрузка;
* `[ ]` инвентаризация.

### Остатки

* `[ ]` фактический остаток;
* `[ ]` доступный остаток;
* `[ ]` резерв;
* `[ ]` остаток по партиям;
* `[ ]` остаток по складам;
* `[ ]` остаток по характеристикам;
* `[ ]` незавершенное производство.

## Контрольная точка

Система показывает фактические и доступные остатки, резервы под заказы и дефицит материалов.

---

# Этап 7. Закупки и снабжение

## Цель

Автоматически определять дефицит материалов и контролировать поставки.

## Необходимо реализовать

* `[ ]` расчет потребности по заказу;
* `[ ]` расчет потребности по производственному плану;
* `[ ]` сравнение с остатками;
* `[ ]` учет резервов;
* `[ ]` формирование дефицита;
* `[ ]` заказ поставщику;
* `[ ]` согласование закупки;
* `[ ]` контроль срока поставки;
* `[ ]` поступление;
* `[ ]` расхождения;
* `[ ]` возврат поставщику;
* `[ ]` история закупочных цен;
* `[ ]` рейтинг поставщиков;
* `[ ]` минимальный остаток;
* `[ ]` уведомления о дефиците.

## Контрольная точка

Закупщик видит, какие материалы, в каком количестве и к какой дате необходимо заказать.

---

# Этап 8. Отгрузка и платежи

## Цель

Завершить операционный цикл заказа после производства.

## Необходимо реализовать

### Оплаты

* `[ ]` план оплат;
* `[ ]` предоплата;
* `[ ]` промежуточная оплата;
* `[ ]` окончательная оплата;
* `[ ]` возврат;
* `[ ]` задолженность;
* `[ ]` просрочка;
* `[ ]` организация-получатель;
* `[ ]` связь с банковской операцией.

### Отгрузки

* `[ ]` готовность к отгрузке;
* `[ ]` проверка оплаты;
* `[ ]` частичная отгрузка;
* `[ ]` упаковочные места;
* `[ ]` вес;
* `[ ]` перевозчик;
* `[ ]` адрес;
* `[ ]` трек-номер;
* `[ ]` статус доставки;
* `[ ]` возврат;
* `[ ]` документы.

### Печатные формы

* `[ ]` счет;
* `[ ]` накладная;
* `[ ]` УПД;
* `[ ]` акт;
* `[ ]` упаковочный лист;
* `[ ]` этикетки;
* `[ ]` маршрутный лист.

## Контрольная точка

Заказ проходит путь от лида до производства, полной оплаты и отгрузки, а все этапы фиксируются в системе.

---

# Этап 9. Себестоимость и аналитика

## Цель

Получать фактическую прибыльность каждого заказа и управленческие показатели компании.

## Текущее состояние

* `[x]` создан dashboard продаж;
* `[x]` реализованы KPI;
* `[x]` реализована воронка;
* `[x]` реализована динамика;
* `[x]` реализована аналитика источников;
* `[x]` реализованы фильтры;
* `[~]` данные dashboard демонстрационные;
* `[ ]` dashboard не использует фактические backend-данные;
* `[ ]` производственная и финансовая аналитика отсутствуют.

## Необходимо реализовать

### Интеграция dashboard

* `[ ]` подключить реальные CRM-данные;
* `[ ]` подключить реальные заказы;
* `[ ]` подключить реальные задачи;
* `[ ]` подключить реальные активности;
* `[ ]` добавить серверную агрегацию;
* `[ ]` добавить права доступа к показателям.

### Себестоимость

* `[ ]` плановые материалы;
* `[ ]` фактические материалы;
* `[ ]` печать;
* `[ ]` крой;
* `[ ]` пошив;
* `[ ]` упаковка;
* `[ ]` брак;
* `[ ]` переделка;
* `[ ]` доставка;
* `[ ]` сторонние услуги;
* `[ ]` дополнительные расходы;
* `[ ]` план-факт;
* `[ ]` маржа заказа.

### Отчеты

* `[ ]` продажи по менеджерам;
* `[ ]` продажи по источникам;
* `[ ]` конверсия;
* `[ ]` причины потерь;
* `[ ]` прибыль по заказам;
* `[ ]` прибыль по моделям;
* `[ ]` загрузка производства;
* `[ ]` производительность;
* `[ ]` брак;
* `[ ]` остатки;
* `[ ]` дефицит;
* `[ ]` задолженность;
* `[ ]` движение денежных средств;
* `[ ]` панель руководителя.

## Контрольная точка

Руководитель видит реальные продажи, загрузку производства, задолженность, себестоимость и прибыль по каждому заказу.

---

# Этап 10. Пользователи, интеграции и промышленная эксплуатация

## Цель

Подготовить систему к ежедневной работе сотрудников из разных подразделений и локаций.

## Пользователи и безопасность

* `[ ]` авторизация;
* `[ ]` восстановление пароля;
* `[ ]` сотрудники;
* `[ ]` подразделения;
* `[ ]` роли;
* `[ ]` права доступа;
* `[ ]` ограничения по организациям;
* `[ ]` журнал входов;
* `[ ]` журнал изменений;
* `[ ]` аудит действий.

## Интеграции

Передача в 1С должна охватывать номенклатуры, заказы, утверждённые спецификации, списание материалов, выполненные операции, выпуск продукции и связанные документы.

* `[ ]` сайт;
* `[ ]` email;
* `[ ]` телефония;
* `[ ]` Telegram;
* `[ ]` WhatsApp;
* `[ ]` VK;
* `[ ]` Google Sheets;
* `[ ]` Excel;
* `[ ]` 1С;
* `[ ]` банковские выписки;
* `[ ]` службы доставки.

## Инфраструктура

* `[ ]` production Docker Compose;
* `[ ]` VPS;
* `[ ]` HTTPS;
* `[ ]` домен;
* `[ ]` reverse proxy;
* `[ ]` резервное копирование;
* `[ ]` восстановление;
* `[ ]` файловое хранилище;
* `[ ]` CI/CD;
* `[ ]` мониторинг;
* `[ ]` уведомления об ошибках;
* `[ ]` staging-среда;
* `[ ]` production-среда;
* `[ ]` инструкции пользователям.

## Контрольная точка

Sport-Lead развернут на сервере, защищен ролями и правами, имеет резервное копирование и доступен сотрудникам для ежедневной операционной работы.

---

# Приоритет разработки после v0.3.0-demo

```text
Стабилизация текущего demo
        ↓
Единая CRM-модель данных
        ↓
Backend API лидов, сделок и клиентов
        ↓
Подключение frontend к backend
        ↓
Сохранение Kanban и форм
        ↓
Конвертация Lead → Deal → Order
        ↓
Заказ покупателя
        ↓
Модели, лекала и техкарты
        ↓
Производство
        ↓
Склад и закупки
        ↓
Отгрузка и платежи
        ↓
Себестоимость и управленческая аналитика
```

---

# Следующий целевой этап

## Предлагаемая версия: `v0.4.0`

Версия еще не выпущена.

### Главная цель

Перевести модуль продаж с демонстрационных данных на реальный backend.

### Обязательный объем

* `[ ]` единая модель `Lead`;
* `[ ]` единая модель `Deal`;
* `[ ]` единая модель `Client`;
* `[ ]` миграции;
* `[ ]` CRUD API;
* `[ ]` подключение frontend;
* `[ ]` сохранение Kanban;
* `[ ]` сохранение форм;
* `[ ]` реальные фильтры;
* `[ ]` реальные данные dashboard;
* `[ ]` базовые backend-тесты;
* `[ ]` проверка полного сценария.

### Контрольный пользовательский сценарий

1. Менеджер создает лид.
2. Лид сохраняется в PostgreSQL.
3. Лид появляется в Kanban.
4. Менеджер меняет статус.
5. Статус сохраняется после перезагрузки.
6. Менеджер открывает карточку и редактирует данные.
7. Лид преобразуется в клиента и сделку.
8. Dashboard пересчитывается по реальным данным.

---

# Критерий готовности v0.4.0

Версия считается готовой, когда CRM перестает быть демонстрационным интерфейсом и становится рабочим модулем с реальным хранением данных.

---

# Версионные коды

## Актуальные

* структура: `SL-ERP-CHECK-v0.3.0`
* roadmap: `SL-ROADMAP-v0.3.0`

## Исторические

* структура: `SL-ERP-CHECK-v1`
* roadmap: `SL-ROADMAP-v1`

Исторические коды сохраняются для ссылок на первоначальную архитектуру, но новые задачи должны основываться на версии `v0.3.0`.

## Синхронизация v0.6.1-roadmap-sync

* `[x]` документ сохранён как исторический, а актуальный план вынесен в `SL-ROADMAP-v0.6.1` без переименования или удаления прежнего roadmap; подтверждения: `docs/roadmap/SL-ROADMAP-v0.6.1.md`, `docs/README.md`, `docs/reports/ROADMAP-SYNC-REPORT.md`; проверки: project check 9/9, backend pytest 28, frontend tests 32, TypeScript, lint, build, Compose и `git diff --check`; ограничение: release/tag `v0.6.1` ещё не создан.

## Итерация v0.6.1-lead-city-autocomplete

* `[x]` поля города в создании лида, профиле клиента и коммерческой доставке используют единый необязательный `CityAutocomplete`: подсказки появляются с двух символов, управляются мышью и клавиатурой, а произвольное значение сохраняется обычной строкой; подтверждения: `frontend/components/ui/city-autocomplete.tsx`, `frontend/lib/city-suggestions.ts`, `frontend/lib/sales/city-suggestions.test.mjs`, `frontend/components/sales/lead-create-dialog.tsx`, `frontend/components/sales/lead-customer-details.tsx`, `frontend/components/sales/lead-commercial-details.tsx`; проверки: backend pytest 32, frontend tests 43, TypeScript, ESLint, production build, project check 9/9 и `git diff --check`; ограничения: browser-smoke недоступен, остальные части `patch-0.6-lead-ux-custom-stages` не входят в эту итерацию.

## Итерация v0.6.1-lead-custom-stage-persistence

* `[x]` конфигурация рабочих стадий и позиция API-лида сохраняются в PostgreSQL: `LeadStage` владеет метаданными, `Lead.status` остаётся единственным стабильным ID текущей стадии/финального состояния, а отключение или удаление custom-стадии атомарно переносит связанные лиды; подтверждения: `backend/app/models/sales.py`, `backend/app/services/lead_stages.py`, `backend/app/api/lead_stages.py`, `backend/alembic/versions/e4b8a2c91d7f_add_persistent_lead_stages.py`, `backend/tests/test_lead_stages.py`, `frontend/lib/sales/lead-stage-api.ts`, `frontend/components/sales/lead-workspace.tsx`; проверки: backend pytest 34, frontend tests 45, TypeScript, ESLint, production build, OpenAPI, Alembic upgrade/downgrade/upgrade/check, project check 9/9 и `git diff --check`; ограничения: встроенный browser недоступен, detail-карточка стадий, финальные действия и расширенная drag-область остаются следующими пунктами патча.

## Итерация v0.6.1-lead-detail-custom-stages

* `[x]` числовая карточка лида загружает общий backend-список стадий, сохраняет custom ID через прежний PATCH и после reload показывает ту же стадию, что и Kanban; фиксированный frontend allowlist удалён, API-ошибка не подменяется `localStorage`; подтверждения: `frontend/app/(workspace)/sales/leads/[leadId]/page.tsx`, `frontend/components/sales/lead-page.tsx`, `frontend/components/sales/lead-header.tsx`, `frontend/lib/sales/lead-details.ts`, `frontend/lib/sales/lead-detail-stage.test.mjs`; проверки: backend pytest 34, frontend tests 47, TypeScript, ESLint, production build, project check 9/9 и `git diff --check`; ограничения: browser-smoke недоступен, финальные действия и расширенная drag-область ещё не реализованы.

## Итерация v0.6.1-lead-detail-final-actions

* `[x]` шкала detail-карточки завершена отдельными действиями «Оформить заказ» и «Закрыть с отказом»: они не входят в `LeadStage`, открывают существующий completion dialog и вызывают прежние `POST /leads/{lead_id}/convert` и `/reject`, а успешный результат сразу показывает завершение и ссылку на заказ либо причину отказа; подтверждения: `frontend/components/sales/lead-header.tsx`, `frontend/components/sales/lead-page.tsx`, `frontend/components/sales/lead-completion-dialog.tsx`, `frontend/lib/sales/lead-final-actions.test.mjs`; проверки: backend pytest 34, frontend tests 48, TypeScript, ESLint, production build, project check 9/9 и `git diff --check`; ограничения: browser-smoke недоступен, расширенная drag-область Kanban остаётся последним пунктом UX-патча.

## Итерация v0.6.1-kanban-full-card-drag

* `[x]` общая Kanban-карточка стала drag-областью целиком: custom pointer sensor защищает кнопки и служебные интерактивные элементы, но разрешает drag за заголовочную ссылку после порога 6 px; обычный click открывает `card.href`, click после drop подавляется, grip остаётся визуальным индикатором; подтверждения: `frontend/components/kanban/kanban-board.tsx`, `frontend/components/kanban/kanban-card.tsx`, `frontend/components/kanban/kanban-interaction.ts`, `frontend/components/kanban/kanban-interaction.test.mjs`; проверки: backend pytest 34, frontend tests 50, TypeScript, ESLint, production build, project check 9/9 и `git diff --check`; ограничение: интерактивный browser-smoke недоступен, программно проверяемый объём `patch-0.6-lead-ux-custom-stages` завершён.

## Итерация v0.6.1-lead-duplicate-candidates

* `[~]` поиск и подготовка дедупликации лидов реализованы первым backend-инкрементом: `GET /leads/duplicate-candidates` ищет кандидатов по email и нормализованному телефону в существующих `Lead` и `LeadContact`, возвращает `LeadRead`, поддерживает исключение текущего лида и не создаёт новую модель данных; подтверждения: `backend/app/services/lead_duplicates.py`, `backend/app/api/leads.py`, `backend/tests/test_lead_duplicates.py`; проверки: backend pytest 36 и project check 9/9 с `PYTHONUTF8=1`; ограничения: создание лида пока не блокируется, frontend-предупреждение и слияние дублей остаются незавершёнными.

## Итерация v0.6.1-lead-card-save-fix

* `[x]` критический дефект сохранения detail-карточки устранён для числовых API-лидов: полный коммерческий блок отправляется через существующий `PATCH /leads/{lead_id}`, хранится в `Lead`, возвращается через `LeadRead` и восстанавливается после reload; `delivery_city` отделён от `city`, а frontend state после save берётся из backend-ответа; подтверждения: `backend/app/models/sales.py`, `backend/app/schemas/sales.py`, `backend/app/api/leads.py`, `backend/alembic/versions/f2a7c4d8e901_add_lead_card_commercial_fields.py`, `backend/tests/test_lead_conversion.py`, `frontend/lib/sales/lead-commercial-api.ts`, `frontend/lib/sales/lead-commercial-api.test.mjs`, `frontend/components/sales/lead-commercial-details.tsx`, `frontend/app/(workspace)/sales/leads/[leadId]/lead-commercial-actions.ts`; проверки: backend pytest 37, frontend tests 50, TypeScript, ESLint, production build, Alembic upgrade/check, project check 9/9 и `git diff --check`; ограничения: frontend-список backend-пользователей для выбора ответственного не реализован, demo-лиды и local-only задачи/заметки/timeline остаются вне итерации.
