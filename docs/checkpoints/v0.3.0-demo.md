# v0.3.0-demo Recovery Guide

Created: 2026-07-16

## Checkpoint References

- Validated integration commit on `main`: `abfe5705f9e13ae1922ec73d91bed8d840647327`
- Checkpoint commit: resolve with `git rev-parse v0.3.0-demo^{commit}`
- Tag: `v0.3.0-demo`
- Protected recovery branch: `stable/v0.3.0-demo`
- Sales PR: `#1`
- Integration PR: `#2`

The tag and stable branch include this document and point to the final checkpoint commit on `main`.

## Validation

- `npm run lint` and `npm run build` passed; the build included TypeScript compilation and all Sales routes.
- `python scripts/check_project.py` passed 9/9 checks, including Python compilation, FastAPI/OpenAPI, SQLAlchemy models, collectors, Alembic, frontend, and Compose.
- `docker compose config --quiet` and `git diff --check` passed.
- No standalone frontend typecheck/test scripts or project-configured pytest/Ruff commands are present.
- User acceptance covered the Sales UI and repeated Kanban drag-and-drop without `Maximum update depth exceeded`.

## Known Limitations

Sales uses in-memory demo data. Kanban changes do not survive reloads. CRM backend integration, authentication, real-time updates, and external CRM integrations are not implemented.

## Restore or Run

Create a recovery branch without committing directly to the stable branch:

```powershell
git fetch origin --prune --tags
git switch -c recovery/from-v0.3.0-demo v0.3.0-demo
```

Run the frontend and backend in separate terminals:

```powershell
cd frontend
npm install
npm run dev
```

```powershell
cd backend
uvicorn app.main:app --reload
```

Recheck the project and return to current `main`:

```powershell
python scripts/check_project.py
git switch main
git pull --ff-only
```

Do not commit directly to `stable/v0.3.0-demo`; branch from the tag for recovery work.
