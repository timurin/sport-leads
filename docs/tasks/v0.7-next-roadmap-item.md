v0.7.x — Следующий пункт roadmap
Контекст

Итерация товарных позиций заказа завершена, визуально проверена, принята и отправлена в репозиторий.

Цель

Самостоятельно определить и реализовать следующий незакрытый пункт актуального roadmap по модулю «Заказы покупателей».

Перед началом

Проверить:

git status;
текущую ветку и последние коммиты;
AGENTS.md;
roadmap;
ERP-check;
оба HTML-статуса;
фактическое состояние кода, API и тестов.
Объём
выбрать один следующий законченный пункт;
реализовать его полностью;
не брать несколько крупных блоков;
использовать существующую архитектуру;
добавить необходимые тесты;
обновить roadmap, ERP-check и оба HTML-статуса.
Ограничения
не выходить за рамки выбранного пункта;
не начинать следующий пункт после завершения;
не делать крупный рефакторинг без необходимости;
не менять дизайн без функциональной причины;
commit и push не выполнять.
Проверки
backend tests;
frontend tests;
TypeScript;
ESLint;
production build;
Alembic downgrade/upgrade/check, если есть миграция;
project check;
git diff --check.
Критерии готовности
выбранный пункт roadmap реализован полностью;
backend и frontend согласованы;
persistence работает после reload;
тесты и сборка проходят;
документация обновлена;
P0/P1 отсутствуют;
commit не создан.
Итоговый отчёт

Кратко указать:

выбранный пункт;
почему он следующий;
что реализовано;
основные файлы;
миграции и API;
результаты проверок;
что проверить вручную;
ограничения;
следующий рекомендуемый пункт.

После отчёта остановиться.