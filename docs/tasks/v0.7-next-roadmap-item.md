v0.7.x — Следующий пункт roadmap
Контекст

Предыдущая итерация завершена, визуально проверена, закоммичена и отправлена в репозиторий.

Готово:

список заказов покупателей;
read-only карточка заказа;
открытие заказа из списка;
Kanban заказов;
сохранение статуса заказа в backend;
восстановление карточки при ошибке;
сохранение после reload.
Цель

Самостоятельно определить и реализовать следующий незакрытый пункт актуального roadmap для модуля «Заказы покупателей».

Перед началом

Проверить:

git status;
текущую ветку;
последние коммиты;
AGENTS.md;
docs/roadmap/;
docs/architecture/;
docs/erp/status/project-structure.html;
docs/erp/status/roadmap.html.

Не выбирать следующий пункт по предположению. Сначала подтвердить фактический статус по коду, API, тестам и документации.

Объём
выбрать один следующий логичный пункт roadmap;
реализовать его как законченную итерацию;
не брать несколько крупных функций одновременно;
использовать существующую архитектуру;
добавить необходимые тесты;
обновить roadmap, ERP-check и оба HTML-статуса.
Ограничения
не выходить за рамки выбранного пункта;
не начинать следующий пункт после завершения;
не выполнять масштабный рефакторинг без необходимости;
не менять дизайн без функциональной причины;
не удалять полезные файлы;
commit и push не выполнять.
Проверки

Обязательно выполнить актуальные проверки проекта:

backend tests;
frontend tests;
TypeScript;
ESLint;
production build;
project check;
git diff --check.
Критерии готовности
выбранный пункт roadmap реализован полностью;
backend и frontend согласованы;
данные сохраняются после reload, если пункт связан с persistence;
добавлены тесты;
документация и HTML-статусы обновлены;
P0/P1 отсутствуют;
commit не создан.
Итоговый отчёт

Кратко указать:

какой пункт roadmap выбран;
почему он следующий;
что реализовано;
основные изменённые файлы;
результаты проверок;
известные ограничения;
что проверить вручную;
следующий рекомендуемый пункт.

После отчёта остановиться.