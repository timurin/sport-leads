# v0.8.8b — Card layout, tabs and visual fix

## Цель

Привести карточку номенклатуры к утверждённому HTML-эталону и исправить структуру страницы.

## Сделать

- убрать верхний блок страницы:
  - «Настройки»;
  - «Карточка номенклатуры»;
  - «Просмотр и редактирование persistent-карточки»;
- начинать страницу сразу с кнопки «Назад к номенклатуре» и шапки карточки;
- реализовать рабочие вкладки:
  - Основное;
  - Реквизиты;
  - Характеристики и варианты;
  - Изображения и файлы;
  - История;
- при выборе вкладки показывать только соответствующий контент;
- остальные секции скрывать;
- активную вкладку визуально выделять как в HTML-эталоне;
- сохранить корректный URL карточки;
- проверить mobile/tablet/desktop.

## Визуальная проверка

Сверить с `docs/design/nomenclature-card-reference-v1.html`:

- шрифт;
- размеры текста;
- межстрочные интервалы;
- цвета;
- ширины колонок;
- отступы;
- границы;
- тени;
- радиусы;
- размеры кнопок;
- высоту полей;
- табы;
- таблицы.

Не использовать приблизительные стили, если значения уже есть в HTML-эталоне.

## Ограничения

- backend не менять;
- API не менять;
- бизнес-логику не переписывать;
- media и сохранение пока не исправлять;
- другие страницы не менять.

## Проверки

- frontend tests;
- TypeScript;
- ESLint;
- production build;
- 1920, 1440, 1366, 1024, 768, 390 и 360 px;
- git diff --check.

Commit и push не выполнять.