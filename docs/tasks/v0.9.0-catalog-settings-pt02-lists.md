# v0.9.0 — Catalog settings lists (PT-02 adopters)

**Roadmap:** `5.5.2.5`  
**Branch:** `feature/v0.8.1-nomenclature-core`  
**Template:** `DS-PT-02` (full-bleed strip + `DataTable`, same shell as nomenclature list)

## Scope

Migrate nomenclature-section catalog **list** routes off legacy `product-characteristics-*` CSS and ad-hoc tables:

- `/settings/catalogs/product-characteristics`
- `/settings/catalogs/units-of-measure`
- `/settings/catalogs/nomenclature-categories`
- `/settings/catalogs/nomenclature-types`

Does not change PT-05 characteristic **card** (`[characteristicId]`).

## Done when

- All four use `PageLayout` + (`NomenclatureSectionCreateHost` where create applies) + `FilterToolbar` / flush `DataTableFrame`
- Left `EditDrawer` for row edit (save/cancel); `nomenclature-types` is read-only info panel
- `tsc` + `lint` pass
- Roadmap `5.5.2.5` closed after owner spot-check
