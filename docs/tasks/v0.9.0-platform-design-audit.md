# v0.9.0 — Platform Design Audit and Roadmap Stage Insertion

## Выбранный roadmap-пункт

Новый этап:

```
Stage 5 — Design System and Platform Templates
```

Первая микрозадача:

```
5.1.1.1 — Audit existing routes, layouts and page types
```

## Контекст

Задача `docs/tasks/v0.8.9-global-roadmap-rebuild.md` завершена.

Канонический roadmap:

```
docs/roadmap/roadmap.md
```

HTML-roadmap:

```
docs/erp/status/roadmap.html
```

Каноническая структура:

```
docs/architecture/project-structure.md
```

Правила проекта:

```
AGENTS.md
```

Текущий roadmap содержит 18 этапов и назначает Stage 5 началом модуля моделей изделий.

До начала моделей принято архитектурное решение внедрить централизованную дизайн-систему и набор платформенных шаблонов страниц. Поэтому существующий Stage 5 и все последующие этапы необходимо сдвинуть на один номер.

Production-интерфейс в этой задаче не переделывается.

## Цель

1. Вставить перед моделями новый Stage 5 — Design System and Platform Templates.
2. Последовательно перенумеровать все текущие этапы 5–17 в 6–18.
3. Обновить зависимости и коды ссылок roadmap.
4. Провести аудит существующих frontend-маршрутов, layouts и типов страниц.
5. Определить централизованные шаблоны платформы.
6. Подготовить последовательность дизайн-микрозадач.
7. Синхронизировать Markdown-roadmap и HTML-roadmap.
8. Не изменять production frontend и backend.

## Канонические пути

```
AGENTS.md
docs/roadmap/roadmap.md
docs/erp/status/roadmap.html
docs/architecture/project-structure.md
docs/architecture/erp-check.md
docs/design-system/
docs/tasks/
```

Frontend:

```
frontend/app/
frontend/app/(workspace)/
frontend/components/
frontend/lib/
frontend/types/
```

Backend:

```
backend/app/
backend/alembic/
backend/tests/
```

## 1. Проверить состояние репозитория

Выполнить:

```
git status
git branch --show-current
git log -1 --oneline
git diff --name-only
git ls-files "docs/roadmap/roadmap.md"
git ls-files "docs/erp/status/roadmap.html"
git ls-files "docs/architecture/project-structure.md"
git ls-files "docs/design-system/*"
git ls-files "frontend/app/**/page.tsx"
git ls-files "frontend/app/**/layout.tsx"
git ls-files "frontend/app/**/loading.tsx"
git ls-files "frontend/app/**/error.tsx"
git ls-files "frontend/components/**/*.tsx"
```

Учесть, что в worktree могут оставаться изменения предыдущих документационных итераций.

Не перезаписывать и не присваивать текущей задаче изменения:

```
docs/architecture/project-structure.md
docs/erp/status/project-structure.html
```

если они относятся к `v0.8.6`.

## 2. Вставить новый Stage 5

В `docs/roadmap/roadmap.md` добавить перед текущим этапом моделей:

# Stage 5 — Design System and Platform Templates

Цель этапа:

Создать единый визуальный и компоновочный фундамент платформы, чтобы новые модули строились на утверждённых шаблонах, а существующие страницы постепенно мигрировали без повторного проектирования интерфейса.

## 5.1 — Audit and inventory

### 5.1.1 — Routes and page types

Цель:

Получить фактическую карту frontend-маршрутов и классифицировать страницы платформы.

Микрозадачи:

* [ ] 5.1.1.1 — Audit existing routes, layouts and page types
* [ ] 5.1.1.2 — Audit loading, error and empty states
* [ ] 5.1.1.3 — Audit persistent versus demo/local data
* [ ] 5.1.1.4 — Document reference and migration pages

### 5.1.2 — Component inventory

* [ ] 5.1.2.1 — Inventory shared UI components
* [ ] 5.1.2.2 — Inventory domain components
* [ ] 5.1.2.3 — Identify duplicates and overlapping responsibilities
* [ ] 5.1.2.4 — Define keep, unify, replace and deprecate decisions

### 5.1.3 — Layout and scrolling audit

* [ ] 5.1.3.1 — Audit AppShell and workspace layouts
* [ ] 5.1.3.2 — Audit page widths and content containers
* [ ] 5.1.3.3 — Audit nested and double scrolling
* [ ] 5.1.3.4 — Audit sticky and fixed elements
* [ ] 5.1.3.5 — Define target scrolling rules

### 5.1.4 — Responsive audit

* [x] 5.1.4.1 — Define responsive verification matrix
* [x] 5.1.4.2 — Audit desktop layouts
* [x] 5.1.4.3 — Audit laptop layouts
* [x] 5.1.4.4 — Audit tablet layouts
* [x] 5.1.4.5 — Audit mobile layouts
* [x] 5.1.4.6 — Register visual bug microtasks

## 5.2 — Design tokens

### 5.2.1 — Visual foundations

* [x] 5.2.1.1 — Audit existing token sources
* [x] 5.2.1.2 — Define semantic color tokens
* [x] 5.2.1.3 — Define typography scale
* [x] 5.2.1.4 — Define spacing scale
* [x] 5.2.1.5 — Define borders, radius and shadows
* [x] 5.2.1.6 — Define component sizes
* [x] 5.2.1.7 — Define interaction states

### 5.2.2 — Responsive and layer tokens

* [x] 5.2.2.1 — Define breakpoints
* [x] 5.2.2.2 — Define content width rules
* [x] 5.2.2.3 — Define z-index layers
* [x] 5.2.2.4 — Define motion rules
* [x] 5.2.2.5 — Prepare token migration plan

## 5.3 — Platform shell

### 5.3.1 — Navigation shell

* [x] 5.3.1.1 — Standardize sidebar
* [x] 5.3.1.2 — Standardize topbar
* [x] 5.3.1.3 — Standardize workspace tabs
* [x] 5.3.1.4 — Define responsive navigation

### 5.3.2 — Page shell

* [x] 5.3.2.1 — Standardize PageLayout — `PageLayout` / `DS-PAGE-01`; evidence: `docs/design-system/shell-page-layout-standardization.md`
* [x] 5.3.2.2 — Standardize PageHeader — canonical `PageToolbar` / `DS-PAGE-02`; evidence: `docs/design-system/shell-page-header-standardization.md`
* [x] 5.3.2.3 — Standardize page actions — `DS-PAGE-03`; evidence: `docs/design-system/shell-page-actions-standardization.md`
* [x] 5.3.2.4 — Standardize content containers — `DS-PAGE-04`; evidence: `docs/design-system/shell-content-containers-standardization.md`
* [x] 5.3.2.5 — Standardize scrolling ownership — `DS-PAGE-05`; evidence: `docs/design-system/shell-scrolling-ownership.md`
* [x] 5.3.2.6 — Add shared loading and error boundaries — `DS-PAGE-06`; evidence: `docs/design-system/shell-page-state-boundaries.md`

## 5.4 — Shared UI components

### 5.4.1 — Forms

* [x] 5.4.1.1 — Text and numeric inputs — `Input`/`Textarea`; evidence: `docs/design-system/form-controls-standardization.md`
* [x] 5.4.1.2 — Select and combobox — `Select` + CityAutocomplete chrome
* [x] 5.4.1.3 — Checkbox, radio and switch
* [x] 5.4.1.4 — Date and money controls — `DateInput`/`MoneyInput`
* [x] 5.4.1.5 — Validation and help states — `Field` help/error
* [x] 5.4.1.6 — Disabled and read-only states — owner visual OK (`2026-07-21`)

### 5.4.2 — Actions and feedback

* [x] 5.4.2.1 — Buttons and icon actions — `DS-ACTION-01`; evidence: `docs/design-system/actions-buttons-standardization.md`
* [x] 5.4.2.2 — Status badges — `DS-BADGE-01`; evidence: `docs/design-system/status-badges-standardization.md`
* [x] 5.4.2.3.1 — Adopt create inspector/drawer as platform standard
* [x] 5.4.2.3.2 — Extract shared CreateDrawer shell
* [x] 5.4.2.3.3 — Migrate nomenclature create to CreateDrawer
* [x] 5.4.2.3.4 — Migrate lead create to CreateDrawer
* [x] 5.4.2.3.5 — Migrate order/deal/task create (replace DemoActionDialog)
* [x] 5.4.2.3.6 — Migrate remaining nomenclature-section catalog creates
* [x] 5.4.2.3.7 — Define modal-vs-drawer boundaries and visual verification — evidence: `docs/design-system/create-modal-drawer-boundaries.md`; owner visual OK (`2026-07-21`)
* [x] 5.4.2.4 — Toast and inline feedback — `DS-FEEDBACK-01`
* [x] 5.4.2.5 — Loading, empty and error states — `DS-FEEDBACK-02`; owner visual OK (`2026-07-21`) for `5.4.2`

### 5.4.3 — Data presentation

* [x] 5.4.3.1 — Table foundation — `DS-TABLE-01`
* [x] 5.4.3.2 — Filter toolbar — `DS-FILTER-01`
* [x] 5.4.3.3 — Pagination and totals — `DS-LIST-01`
* [x] 5.4.3.4 — Tabs and compact tabs — `DS-TABS-01`
* [x] 5.4.3.5 — Activity timeline — `DS-TIMELINE-01`
* [x] 5.4.3.6 — Tasks and comments panels — `DS-PANEL-01`
* [x] 5.4.3.7 — Entity links and inline editing — `DS-LINK-01`; evidence: `docs/design-system/data-presentation-standardization.md`; owner visual OK (`2026-07-21`)

## 5.5 — Page templates

### 5.5.1 — PT-01 Dashboard

* [ ] 5.5.1.1 — Define template contract
* [ ] 5.5.1.2 — Implement reference layout
* [ ] 5.5.1.3 — Add responsive rules
* [ ] 5.5.1.4 — Verify on Sales Dashboard

### 5.5.2 — PT-02 List/Table Workspace

* [ ] 5.5.2.1 — Define template contract
* [ ] 5.5.2.2 — Implement reference layout
* [ ] 5.5.2.3 — Add responsive table behaviour
* [ ] 5.5.2.4 — Verify on organizations or clients

### 5.5.3 — PT-03 Kanban Workspace

* [ ] 5.5.3.1 — Define template contract
* [ ] 5.5.3.2 — Standardize board structure
* [ ] 5.5.3.3 — Define mobile fallback
* [ ] 5.5.3.4 — Verify on Leads Kanban

### 5.5.4 — PT-04 Tree + List Workspace

* [ ] 5.5.4.1 — Define template contract
* [ ] 5.5.4.2 — Standardize tree and content panes
* [ ] 5.5.4.3 — Add responsive tree drawer
* [ ] 5.5.4.4 — Verify on Nomenclature Workspace

### 5.5.5 — PT-05 Simple Entity Card

* [ ] 5.5.5.1 — Define template contract
* [ ] 5.5.5.2 — Implement reference card
* [ ] 5.5.5.3 — Add responsive layout
* [ ] 5.5.5.4 — Verify on organization or client

### 5.5.6 — PT-06 Complex Entity Card

* [ ] 5.5.6.1 — Define template contract
* [ ] 5.5.6.2 — Standardize entity header
* [ ] 5.5.6.3 — Standardize stage and metrics area
* [ ] 5.5.6.4 — Standardize section grid
* [ ] 5.5.6.5 — Standardize activity tabs
* [ ] 5.5.6.6 — Define responsive collapse
* [ ] 5.5.6.7 — Verify on Lead Card

### 5.5.7 — PT-07 Document Card

* [ ] 5.5.7.1 — Define template contract
* [ ] 5.5.7.2 — Standardize document header
* [ ] 5.5.7.3 — Standardize tabular section
* [ ] 5.5.7.4 — Standardize totals and actions
* [ ] 5.5.7.5 — Define responsive behaviour
* [ ] 5.5.7.6 — Verify on Customer Order Card

### 5.5.8 — PT-08 Versioned Workspace

* [ ] 5.5.8.1 — Define template contract
* [ ] 5.5.8.2 — Define active version and history
* [ ] 5.5.8.3 — Define draft and published states
* [ ] 5.5.8.4 — Define compare and restore UX
* [ ] 5.5.8.5 — Prepare reference Model Card

## 5.6 — Reference migrations

* [ ] 5.6.1 — Migrate Sales Dashboard
* [ ] 5.6.2 — Migrate Leads Kanban
* [ ] 5.6.3 — Migrate Lead Card
* [ ] 5.6.4 — Migrate Customer Order Card
* [ ] 5.6.5 — Migrate Nomenclature Workspace
* [ ] 5.6.6 — Migrate Nomenclature Card
* [ ] 5.6.7 — Create reference Model Card shell

Каждая миграция должна быть отдельным функциональным пунктом с собственными микрозадачами и визуальной проверкой.

## 5.7 — Responsive and accessibility verification

* [ ] 5.7.1 — Desktop matrix
* [ ] 5.7.2 — Laptop matrix
* [ ] 5.7.3 — Tablet matrix
* [ ] 5.7.4 — Mobile matrix
* [ ] 5.7.5 — Horizontal overflow verification
* [ ] 5.7.6 — Keyboard navigation
* [ ] 5.7.7 — Focus visibility
* [ ] 5.7.8 — Contrast verification
* [ ] 5.7.9 — Visual regression checklist

## 5.8 — Design checkpoint

* [ ] 5.8.1 — Design documentation complete
* [ ] 5.8.2 — Tokens approved
* [ ] 5.8.3 — Platform shell approved
* [ ] 5.8.4 — Page templates approved
* [ ] 5.8.5 — Reference pages approved
* [ ] 5.8.6 — Critical visual bugs fixed
* [ ] 5.8.7 — New modules required to use templates

## 3. Перенумеровать последующие этапы

Текущий Stage 5 и все последующие этапы сдвинуть:

```
Старый Stage 5  → новый Stage 6
Старый Stage 6  → новый Stage 7
Старый Stage 7  → новый Stage 8
Старый Stage 8  → новый Stage 9
Старый Stage 9  → новый Stage 10
Старый Stage 10 → новый Stage 11
Старый Stage 11 → новый Stage 12
Старый Stage 12 → новый Stage 13
Старый Stage 13 → новый Stage 14
Старый Stage 14 → новый Stage 15
Старый Stage 15 → новый Stage 16
Старый Stage 16 → новый Stage 17
Старый Stage 17 → новый Stage 18
```

Перенумеровать внутри этих этапов:

* block IDs;
* functional item IDs;
* microtask IDs;
* зависимости;
* текстовые ссылки;
* якоря HTML;
* navigation IDs;
* data attributes;
* отображаемые номера.

Не менять смысл, статус или порядок существующих пунктов, кроме необходимого сдвига номера.

После перенумерации первой микрозадачей моделей должна стать задача Stage 6, а не Stage 5.

## 4. Выполнить только микрозадачу 5.1.1.1

В рамках этой итерации выполнить:

```
5.1.1.1 — Audit existing routes, layouts and page types
```

Не выполнять остальные микрозадачи Stage 5.

### Требуемый аудит

Получить фактический список страниц из:

```
frontend/app/**/page.tsx
```

Для каждой страницы указать:

* URL;
* фактический путь файла;
* route group;
* layout;
* модуль;
* тип страницы;
* основные компоненты;
* источник данных;
* persistent, demo или mixed;
* наличие loading;
* наличие error;
* наличие empty state;
* предполагаемую готовность desktop;
* предполагаемую готовность tablet;
* предполагаемую готовность mobile.

Route group `(workspace)` не включать в URL.

Пример:

```
URL: /sales/leads
File: frontend/app/(workspace)/sales/leads/page.tsx
```

## 5. Классификация страниц

Использовать типы:

* Dashboard;
* Kanban Workspace;
* List/Table Workspace;
* Tree + List Workspace;
* Simple Entity Card;
* Complex Entity Card;
* Document Card;
* Versioned Workspace;
* Settings Page;
* Redirect;
* Placeholder;
* Demo/Local Page.

Если страница подходит к нескольким типам, выбрать основной и указать дополнительный тип отдельно.

## 6. Документация аудита

Создать или обновить:

```
docs/design-system/ui-audit.md
```

Документ должен содержать:

1. дату, версию, ветку и Git SHA;
2. метод проверки;
3. полную таблицу URL ↔ file;
4. классификацию страниц;
5. используемые layouts;
6. persistent/demo/mixed статус;
7. наличие loading/error/empty states;
8. предполагаемые responsive-риски;
9. страницы-эталоны;
10. страницы, требующие миграции;
11. найденные пробелы;
12. следующие микрозадачи аудита.

В рамках `5.1.1.1` не проводить полный компонентный, scrolling или responsive аудит. Только зарегистрировать очевидные риски для следующих задач.

## 7. Страницы-эталоны

Проверить фактические пути и определить текущие reference pages для:

* Sales Dashboard;
* Leads Kanban;
* Lead Card;
* Customer Order Card;
* Nomenclature Workspace;
* Nomenclature Card;
* Organizations;
* Employees;
* Materials.

Не использовать предполагаемые пути.

Если маршрут отсутствует в текущем checkout, так и указать.

## 8. AGENTS.md

В `AGENTS.md` добавить или обновить один раздел:

```
## Platform design system
```

Не дублировать существующие правила.

Зафиксировать:

1. Каноническая документация дизайна хранится в `docs/design-system/`.
2. Каждая новая страница должна ссылаться на template ID PT-01—PT-08.
3. До появления утверждённого template ID новая страница использует ближайший reference pattern.
4. Route group `(workspace)` не включается в URL.
5. Demo/local UI явно маркируется и не считается persistent.
6. Новый модуль не создаёт собственный layout без проверки общих шаблонов.
7. Общий компонент изменяется только после проверки его потребителей.
8. Visual bugs оформляются отдельными roadmap-микрозадачами.
9. Визуальная проверка проводится по утверждённой responsive matrix.
10. Итоговый отчёт UI-задачи указывает:

    * template ID;
    * проверенные страницы;
    * проверенные разрешения;
    * найденные visual bugs.

## 9. Обновить HTML-roadmap

Полностью синхронизировать:

```
docs/erp/status/roadmap.html
```

После вставки Stage 5 ожидается:

* 19 stages: Stage 0–18;
* новый Stage 5;
* прежние Stage 5–17 перенумерованы в 6–18;
* Markdown и HTML имеют одинаковые IDs и чекбоксы;
* 5.1.1.1 отмечается `[x]` только после завершённого аудита;
* остальные новые дизайн-микрозадачи остаются `[ ]`.

Не задавать итоговое число чекбоксов заранее. Рассчитать его из фактического результата.

## 10. Project structure

`docs/architecture/project-structure.md` не менять, потому что production-функциональность не изменилась.

В итоговом отчёте указать:

```
Project structure checklist: changes not required.
```

Если добавление design-system этапа требует будущего изменения структуры, зарегистрировать отдельную документационную микрозадачу, но не менять файл сейчас.

## Ограничения

Не изменять:

* production frontend;
* production backend;
* CSS production-интерфейса;
* layouts;
* UI components;
* API;
* models;
* schemas;
* services;
* migrations;
* tests production-кода;
* `docs/architecture/project-structure.md`;
* `docs/erp/status/project-structure.html`;
* `docs/architecture/erp-check.md`.

Разрешены изменения только в:

```
docs/roadmap/roadmap.md
docs/erp/status/roadmap.html
docs/design-system/ui-audit.md
AGENTS.md
```

Не создавать новые frontend-страницы.

Не начинать Stage 6 — Models.

## Проверки

Выполнить:

```
git diff --check
python scripts/check_project.py
```

Если PostgreSQL недоступен на `127.0.0.1:5433`:

* зафиксировать инфраструктурный блокер;
* не изменять Docker или database configuration;
* отдельно указать результаты остальных проверок.

Дополнительно проверить:

* roadmap содержит 19 stages;
* stage numbers идут 0–18 без пропусков;
* существующие Stage 5–17 корректно сдвинуты;
* зависимости после Stage 5 перенумерованы;
* HTML anchors не конфликтуют;
* Markdown и HTML имеют одинаковое число stages;
* Markdown и HTML имеют одинаковое число blocks;
* Markdown и HTML имеют одинаковое число functional items;
* Markdown и HTML имеют одинаковое число `[x]`;
* Markdown и HTML имеют одинаковое число `[ ]`;
* в URL отсутствует `(workspace)`;
* все пути аудита существуют в `git ls-files`;
* production-код не изменён;
* project structure и ERP-check не изменены.

## Критерии готовности

* новый Stage 5 добавлен перед моделями;
* старые Stage 5–17 перенумерованы в 6–18;
* зависимости и ссылки синхронизированы;
* выполнена только микрозадача `5.1.1.1`;
* создан полный route/page audit;
* страницы классифицированы;
* reference pages определены по фактическим путям;
* `AGENTS.md` содержит правила design system;
* roadmap HTML синхронизирован;
* Stage 6 не начинался;
* production-код не изменён.

## Итоговый отчёт

Вывести:

1. выбранный roadmap-пункт;
2. почему он следующий;
3. ветку и HEAD;
4. изменённые файлы;
5. количество stages до и после;
6. таблицу перенумерации этапов;
7. количество найденных frontend routes;
8. таблицу URL ↔ file;
9. классификацию страниц;
10. reference pages;
11. отсутствующие ожидаемые маршруты;
12. persistent/demo/mixed страницы;
13. страницы без loading/error/empty states;
14. выявленные responsive-риски;
15. закрытый чекбокс `5.1.1.1`;
16. следующие незакрытые дизайн-микрозадачи;
17. изменения `AGENTS.md`;
18. parity Markdown ↔ HTML;
19. результат `git diff --check`;
20. результат `python scripts/check_project.py`;
21. инфраструктурные блокеры;
22. подтверждение, что Stage 6, production-код, project structure и ERP-check не изменялись.
