# Task — 1.2.5 Single lead detail data path

**Roadmap:** `1.2.5`  
**Date:** `2026-08-01`

## Goal

Remove the dual lead-card path (`lead-*` demo fixtures vs numeric API IDs). Detail route loads only from persistent API. Actor for notes/tasks/messages uses `SalesUser` directory until production auth (`17.1.1`).

## Microtasks

| Code | Scope |
|------|--------|
| `1.2.5.1` | Contract + task file |
| `1.2.5.2` | `getLeadDetails` API-only; non-numeric → not found |
| `1.2.5.3` | Lead card/header drop demo/`lead-*` local stage & mutation paths; actor from sales-users |
| `1.2.5.4` | Regression + docs sync |
| `1.2.5.5` | Owner visual (API lead card, no fixture IDs) — **STOP** |

## Status

**Closed** — `1.2.5.1`–`1.2.5.5` (owner visual OK `2026-08-01`). Next: `1.3.3`.
