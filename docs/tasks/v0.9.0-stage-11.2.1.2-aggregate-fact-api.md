# Task — `11.2.1.2` Production fact roll-up API

**Date:** `2026-07-30`  
**Roadmap:** `11.2.1.2`

## Goal

Read-only GET roll-up for `ProductionBatch` and `ProductionOrder` per ADR-018 §8.

## Shipped

- Schemas: `ProductionFactRollupRead` (+ performers / materials / operations lines)
- Service: `app/services/production_fact_rollup.py`
- Endpoints:
  - `GET /production-batches/{batch_id}/fact-rollup`
  - `GET /production-orders/{order_id}/fact-rollup`
- Test: `backend/tests/test_production_fact_rollup_11_2_1_2.py`

## Rules enforced

- SoT stays on TC; no writes to PO/Batch
- Empty batch → zeros / empty lists
- Aggregates: performers, duration, scrap/rework, card qty/status, MATERIAL fact/planned, op volumes

## Next

`11.2.1.3` — UI roll-up block on `/production/orders/{id}`
