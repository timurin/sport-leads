# Task — `12.2.2` Post StockDocument + read balances

**Date:** `2026-07-30`  
**Roadmap:** `12.2.2`  
**ADR:** ADR-019 / ADR-012

## Goal

Services + API: create/post складской документ (receipt/issue), читать остатки из posted ledger (± warehouse). Не писать balance на `Nomenclature`.

## Done

- [x] `POST /stock/documents` (create; `post=true` by default)
- [x] `POST /stock/documents/{id}/post` + `GET /stock/documents/{id}`
- [x] Signed ledger qty: receipt `+`, issue `−` (API takes absolute qty)
- [x] `GET /stock/balances` filled from posted `StockLedgerLine`
- [x] Tests: `test_stock_documents_12_2_2.py`

## Next

`12.2.3` — live column on `/warehouse/stock` (balances already from ledger; verify/wire UI)
