# Task — `12.2.3` Live balance column on `/warehouse/stock`

**Date:** `2026-07-30`  
**Roadmap:** `12.2.3`

## Goal

Колонка «Остаток» / фильтр «С остатком» на `/warehouse/stock` читают live `GET /stock/balances` (posted ledger из `12.2.2`).

## Done

- [x] Catalog `loadWarehouseNomenclatureCatalog` → `getNomenclatureStockBalances` (already wired; comments → live `12.2.3`)
- [x] Display format `formatStockBalanceQty` (strip trailing zeros)
- [x] FE tests: `stock-balances-12-2-3.test.mjs`, warehouse route markers
- [x] Backend fill already in `12.2.2` — no API change required

## Next

`12.2.4` — regression tests (post in/out → balance; no balance on Nomenclature)
