# Task — 17.2.1 Production Docker / reverse proxy / HTTPS

**Roadmap:** `17.2.1`  
**Date:** `2026-08-01`

## Boundary

In-repo production contour only. **Live VPS provisioning** (buy/SSH/DNS) is owner ops using the runbook — agents do not touch foreign hosts without explicit credentials and permission.

## Microtasks

| Code | Status |
|------|--------|
| `17.2.1.1` | **done** — Docker images + `compose.prod.yaml` |
| `17.2.1.2` | **done** — Caddy reverse proxy + TLS |
| `17.2.1.3` | **done** — VPS bootstrap runbook |

## Stack shape

```
Internet → Caddy (:80/:443) → Next web (:3000)
                              ↘ (server-side) FastAPI api (:8000) → postgres
```

Browser talks only to Caddy/Next (same-origin cookies via Next login bridge, ADR-023). API and Postgres stay on the internal Docker network.

## Evidence

- `compose.prod.yaml`
- `docker/backend/Dockerfile`, `docker/frontend/Dockerfile`, `docker/Caddyfile`
- `.env.production.example`
- `docs/ops/production-17-2-1.md`

## Next

`17.2.2` — production deploy pipeline / monitoring / log aggregation.
