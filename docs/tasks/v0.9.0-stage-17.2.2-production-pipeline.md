# Task — 17.2.2 Production deploy pipeline / monitoring / logs

**Roadmap:** `17.2.2`  
**Date:** `2026-08-02`  
**Depends on:** `17.2.1` (`compose.prod.yaml`)

## Boundary

In-repo CI deploy workflow + monitoring/log runbook. Live SSH secrets and VPS apply are owner-configured GitHub Secrets — agents do not store host credentials.

## Microtasks

| Code | Status |
|------|--------|
| `17.2.2.1` | **done** — GitHub Actions deploy (workflow_dispatch + SSH) |
| `17.2.2.2` | **done** — Health routes for uptime + check scripts |
| `17.2.2.3` | **done** — JSON log rotation + aggregation runbook |

## Evidence

- `.github/workflows/deploy-production.yml`
- `docker/Caddyfile` — `/health`, `/health/ready` → api
- `compose.prod.yaml` — `json-file` rotation
- `scripts/prod-health-check.ps1`, `scripts/prod-health-check.sh`
- `docs/ops/production-17-2-2.md`

## Next

`17.2.3` — production backup / DR / admin runbooks (extends `scripts/backup_db.ps1` / `restore_db.ps1`).
