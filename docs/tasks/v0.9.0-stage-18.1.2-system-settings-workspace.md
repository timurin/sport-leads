# Task — 18.1.2 System settings workspace

**Roadmap:** `18.1.2`  
**Date:** `2026-08-02`  
**Status:** done — owner visual OK `2026-08-02`

## Scope

Platform-level system settings singleton (not domain catalogs):

| Layer | Path |
|-------|------|
| Model | `backend/app/models/platform_system_settings.py` |
| Migrations | `w0x1y2z3a456`, `x1y2z3a4b567` (logo) |
| API | `GET/PUT /platform-system-settings` + logo + brand |
| Permission | `system_settings.write` (admin) |
| UI | `/settings/system` — `PageToolbar` + `SystemSettingsWorkspace` |

Fields: organization display name (sidebar brand), logo, default timezone, support email, UI locale, notes.

## Visual checklist (owner)

1. Form on `/settings/system` — OK  
2. Persist name/TZ/locale/email/notes — OK  
3. Org name → sidebar brand — OK  
4. Logo upload/delete — OK  
5. `PageToolbar` chrome — OK  
6. Hub «Платформа» — OK  

## Stop

Closed after owner visual OK. Next: `18.1.3`.
