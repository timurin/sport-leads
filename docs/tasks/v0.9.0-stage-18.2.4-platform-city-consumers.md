# Task — 18.2.4 Platform city consumers

**Roadmap:** `18.2.4` (visual pending)  
**Date:** `2026-08-02`

## Scope

- Replace CRM city pickers static suggestions with platform directories API
- Add authenticated Next proxy for city suggestions from backend `platform_directories`
- Add direct cross-link from CRM city pickers to `/settings/platform-directories/cities`
- Keep free-text save behavior; no demo fallback

## Consumer surfaces

1. Lead create drawer — city field
2. Lead card → customer profile — city field
3. Lead card → commercial details — delivery city field

## Visual checklist (owner) — `18.2.4`

1. `/sales/leads` → create lead drawer: city suggestions come from live platform cities
2. `/sales/leads/[leadId]` → customer city suggestions come from live platform cities
3. `/sales/leads/[leadId]` → delivery city suggestions come from live platform cities
4. No matches / API issue state keeps manual text entry and offers link to platform city directory
5. `DS-SHELL-01` / `DS-SHELL-02` visual contract preserved

## Stop

Do not close `18.2.4` until owner visual OK.
