# Stage 18.3.5 - Administration UI for print forms

## Scope

Next unfinished roadmap item after `18.3.4`.

Replace the `/settings/print-forms` stub with live Administration UI on the print-form registry API:

- PT-02 list workspace under `/settings/print-forms`;
- create and open card flow from the registry list;
- PT-05 card/editor under `/settings/print-forms/[printFormId]`;
- metadata save, activate/archive actions;
- version create/publish/current surface and preview panel on the card.

## Constraints

- No demo substitution.
- No shell visual changes; only Stage 18 content area.
- Consumer print integration stays out of this slice (`18.3.6`).
- Roadmap close requires owner visual verification after UI ship.

## Visual checklist

1. `/settings/print-forms` opens live registry list.
2. Create form works from the list header and opens the card.
3. Card saves metadata and updates status through activate/archive.
4. Version create + publish current work from the card.
5. Preview panel renders inline HTML template with JSON payload.
