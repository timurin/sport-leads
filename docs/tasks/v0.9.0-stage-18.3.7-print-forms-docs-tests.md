# Stage 18.3.7 - Print forms documentation checkpoint and regression tests

## Scope

Next unfinished roadmap item after confirmed visual close of `18.3.6`.

Finalize the Stage `18.3` sales-consumer slice:

- sync roadmap and HTML twin for `18.3.6`;
- record the shipped sales print-output contract in canonical print-form docs;
- keep focused regression evidence for sales payload builders and existing registry generate flow.

## Constraints

- No new product behavior beyond the shipped `18.3.6` integration.
- No parallel print contract outside `docs/architecture/print-forms-domain.md`.
- HTML twins must be updated atomically with Markdown.

## Evidence target

1. `docs/architecture/print-forms-domain.md` reflects shipped sales consumer bindings.
2. `frontend/lib/sales/commercial-print.test.mjs` covers order / quotation / invoice payload generation.
3. Existing registry generate regression remains in `backend/tests/test_print_forms_18_3_3.py`.
