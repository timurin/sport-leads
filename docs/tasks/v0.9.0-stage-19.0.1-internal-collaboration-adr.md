# Stage 19.0.1 — Internal collaboration ADR

## Scope

Accept ADR for InternalCollaboration / OrderChat:

- thread anchored to `SalesOrder` with optional `TechnicalCard` message context;
- messages, @mentions, microtasks;
- explicit ≠ CRM `LeadTask` / external `NormalizedMessage` / design asset comments.

## Delivered

- `docs/architecture/decisions/ADR-026-internal-collaboration-order-chat.md` — status принято `2026-08-03`.

## Out of scope

- Persistence, API, UI (`19.1`+).
- Amending order-card field-links / ADR-016 cross-ref (`19.0.2`).
- Dedupe note packaging (`19.0.3`).
