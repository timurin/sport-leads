# Task — 3.2.6 Order-item model selection smoke

**Roadmap:** `3.2.6.1`–`3.2.6.2`  
**Date:** `2026-07-27`  
**Depends:** `3.2.5`, `3.2.7`, `6.1.11`–`6.1.12`, `6.1.17`, `6.2.7`, `6.3.5`  
**Contract:** `docs/architecture/order-item-model-assembly.md`  
**Status:** `3.2.6.1`–`3.2.6.2` done `2026-07-27` (smoke green; no P0/P1)

## Goal

Prove the persistent selection path:

```
PRODUCT nomenclature
  → available-models whitelist
      → ProductModel (autofill article / name / size_type)
          → AssemblyVariant ∈ model active variants
          → ShopRoutingTemplate ∈ model routing whitelist (6.1.17)
```

without Stage 7 document creation. Manager cannot pick a foreign model or foreign routing.

## Microtasks

- [x] `3.2.6.1` — Script or manual smoke checklist (whitelist filter, autofill size_type/article, variant offer, routing offer, reject foreign model/routing) — `v0.9.0`; this task + `tests/test_order_item_model_selection_smoke_3_2_6.py`
- [x] `3.2.6.2` — Fix P0/P1 gaps found in smoke — `v0.9.0`; **none found**

## Automated smoke (primary evidence)

```text
cd backend
python -m pytest tests/test_order_item_model_selection_smoke_3_2_6.py -q
```

Also covered by prior suites:

- `tests/test_order_item_model_assembly.py` — model/assembly ADR-014
- `tests/test_order_item_routing_3_2_7.py` — routing require / foreign / immutability

## Manual UI checklist (order card)

Route: `/sales/orders/[id]` → товарные позиции.

| # | Step | Expected |
|---|------|----------|
| 1 | PRODUCT with empty available-models | Model optional; line saves without model |
| 2 | PRODUCT with non-empty whitelist, no model | Save blocked / prompt to pick model |
| 3 | Pick model from whitelist | Article / name / size_type autofilled on persist |
| 4 | Model with ≥1 active assembly variant | Variant select required; options = that model only |
| 5 | Model with ≥1 active routing link | Routing select required; options = model whitelist only (not full `/routings` catalog) |
| 6 | Attempt foreign model (not on PRODUCT list) | API 400 |
| 7 | Attempt foreign routing (other model’s preset) | API 400 |
| 8 | No Stage 7 Spec created by this path | Spec registry unchanged |

## Out of scope

- Stage 7 specification document creation
- TC generate / apply-routing wire-up (`8.2.3.7`)
- Closing owner visuals for other stages

## Acceptance

- [x] Checklist + automated smoke script exist and pass
- [x] Foreign model / foreign routing rejected on API
- [x] Happy path fills model snapshots + variant + routing when whitelists non-empty
- [x] P0/P1 from smoke addressed in `3.2.6.2` (or none found)
