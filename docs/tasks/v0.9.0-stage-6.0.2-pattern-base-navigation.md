# Task — Stage 6.0.2 Settings navigation contour

**Roadmap:** `6.0.2.1` … `6.0.2.3`  
**Iteration:** `v0.9.0` / `2026-07-22`  
**Depends on:** `6.0.1` / `ADR-014`

## Goal

Users discover models, size grids, and patterns from one settings section; routes exist without 404 shell and without demo catalog data.

## Done in this iteration

- `6.0.2.1` — `pattern-base` group in `frontend/lib/navigation.ts`
- `6.0.2.2` — list/card placeholders under `/settings/catalogs/product-models`, `size-grids`, `patterns`

## Pending owner visual check (`6.0.2.3`)

**Closed** — owner visual OK (`2026-07-22`).

1. Settings → База лекал visible (sidebar + top nav / compact).
2. Links open list shells (EmptyState, no demo rows).
3. Card placeholder URLs resolve (size-grids / patterns); product-models keeps prior PT-08 demo-reference card only as design sample, not as list data.
4. `DS-SHELL-01` / `DS-SHELL-02` visual contract preserved (nav data only).
