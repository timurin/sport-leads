# Task — 9.1.2 Technical card database core

**Roadmap:** `9.1.2.1`–`9.1.2.4`  
**Date:** `2026-07-26`  
**ADR:** `docs/architecture/decisions/ADR-016-technical-card-domain.md`

## Goal

Persistent storage for technical card header, composition links, unit lines, operation volume lines (`TechnicalCardOperationLine`), and stage results.

## Microtasks

- [x] `9.1.2.1` — SQLAlchemy entities (header, composition, unit lines, `TechnicalCardOperationLine`, stage results)
- [x] `9.1.2.2` — Alembic migration upgrade + downgrade (`k2l3m4n5o678`)
- [x] `9.1.2.3` — Pydantic read/write schemas
- [x] `9.1.2.4` — Backend regression tests for persistence

## Soft dependency note (`8.1.3`)

`TechnicalCardOperationLine.tech_operation_id` is a **soft integer** (no FK) until TechOperation catalog ships. Empty op-volume lines / zero volumes allowed; no demo prefill.

## Out of scope

- Generate / lifecycle API (`9.2.*`)
- Prefill from routing / catalog (`9.3.3`)
- UI (`9.4.*`)
- TechOperation catalog CRUD (`8.1.3`)

## Acceptance

- [x] Tables in PostgreSQL via reversible migration
- [x] UNIQUE one card per `sales_order_item_id`
- [x] Tests cover header, unit lines, operation volume lines, stage results
- [x] Schemas round-trip ORM attributes
