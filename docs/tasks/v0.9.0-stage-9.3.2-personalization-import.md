# Task — 9.3.2.5–9.3.2.8 aggregate personalization import

**Roadmap:** `9.3.2.5`–`9.3.2.8`  
**Date:** `2026-07-28`  
**Closed:** `2026-08-01`  
**ADR:** ADR-016 §2 amend (`2026-07-28`)

## Goal

Keep technical-card unit lines as SoT (`N = order quantity`), but allow managers to import personalization in **aggregate** rows by columns and expand them into per-piece unit lines.

## Owner decisions

- Import format is aggregate, not per-piece
- Columns: `size_type`, `size`, `surname`, `print_number`, `quantity`, `notes`
- `surname` maps to `personalization`
- `print_number` stays a separate text field
- `color` is removed from active UI/import; legacy nullable storage may remain until cleanup
- `Σ quantity` of import rows must equal the order-item quantity exactly
- Unit-line `size_type` = `male` / `female` (ProductModel `men`/`women`/`kids` maps on defaults; kids → null)

## Microtasks

- [x] `9.3.2.5` — Amend domain/UI contract: `size_type` + visible fields without `color` — Alembic `o2p3q4r5s678`
- [x] `9.3.2.6` — Backend aggregate import endpoint/service: validate sum and expand into N unit lines
- [x] `9.3.2.7` — Frontend «Поштучно»: aggregate import UX and updated visible columns
- [x] `9.3.2.8` — Regression tests for expand, reject, and round-trip

## Evidence

- `backend/app/services/technical_cards.py` — `import_unit_lines`
- `backend/alembic/versions/o2p3q4r5s678_tc_unit_line_size_type_male_female.py`
- `backend/tests/test_technical_cards_9_3_2.py`
- `frontend/components/production/tech-card-detail-workspace.tsx` — import panel
- `frontend/lib/production/tech-cards.test.mjs`

## Acceptance

- [x] Aggregate import expands into N stored unit lines
- [x] Quantity mismatch is rejected with a clear domain error
- [x] UI no longer shows `color` in active personalization columns
- [x] `size_type` round-trips through API and UI (`male`/`female`)
