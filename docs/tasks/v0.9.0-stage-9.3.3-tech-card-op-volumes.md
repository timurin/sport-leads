# Task — 9.3.3 Operation volumes on card

**Roadmap:** `9.3.3.1`–`9.3.3.4`  
**Date:** `2026-07-26`  
**ADR:** ADR-016 §3

## Goal

Manager-maintained TechOperation volume rows on the technical card; soft prefill until catalog `8.1.3` (empty OK, no demo).

## Microtasks

- [x] `9.3.3.1` — Persist op-volume lines (already in `9.1.2` model; API replace/list)
- [x] `9.3.3.2` — Soft prefill from TechOperation when table exists; else empty no-op
- [x] `9.3.3.3` — API + validation (unique sequence/stage_order; catalog unit check when available; replace only in draft)
- [x] `9.3.3.4` — Regression tests

## API

| Method | Path |
|--------|------|
| GET | `/technical-cards/{id}/operation-lines` |
| PUT | `/technical-cards/{id}/operation-lines` (draft only) |
| PATCH | `/technical-cards/{id}/operation-lines/{line_id}` (volume) |
| POST | `/technical-cards/{id}/operation-lines/prefill` |

Volumes do not bypass stage gates (`9.2.2.2`).

## Out of scope

- TechOperation CRUD (`8.1.3`)
- Stage gates (`9.2.2`)
- UI (`9.4.*`)

## Acceptance

- [x] Persist + round-trip
- [x] Prefill soft / no demo
- [x] Tests: `backend/tests/test_technical_cards_9_3_3.py`
