# Task — v1.00 Stage 0.5 Canonical VPS workflow

**Roadmap:** `0.5.1`–`0.5.12` + `0.5.6.1` (`SL-ROADMAP-v1.00`)  
**Date:** `2026-08-25`  
**Contract code:** `SL-VPS-CANONICAL-v1`  
**ADR:** ADR-032  
**Depends on:** in-repo `17.2.1`–`17.2.3` (do not reopen); LAN `0.3`

## Queue

Product slice `12.4.1.5` inventory UI shipped `2026-08-25` (owner unpaused). Stop at visual `12.4.1.6`. Ops slice **0.5**: git gate `0.5.3` closed; next clone `0.5.4`.

Owner: Ubuntu 26.04 host `0.5.4.1` done; clone `0.5.4`, DNS **`sport-lead.ru`** `0.5.6.1`, then `0.5.6`–`0.5.8`/`0.5.11`–`0.5.12`.

## SoT (ADR-032)

| Asset | Canonical | Agent default |
|-------|-----------|---------------|
| Code | GitHub `main` | local repo; no rsync deploy |
| DB rows | VPS Postgres | Docker Postgres `:5432` |
| User UI | `https://sport-lead.ru` | `:3001` / `:8000` LAN (agent tests only) |
| Media | VPS `./storage` | local `storage/` after pull |
| Secrets | `.env.production` / `.env.tunnel` | never commit |

## Git gate (`0.5.3`) — closed

Inventory + Spec (Alembic `e4f5a6b7c890`) + Stage `0.5` artifacts are on `main` (this commit). VPS clone `0.5.4` is now allowed. Do not commit `__pycache__`.

## Owner inputs (before `0.5.4`)

- VPS IPv4, SSH user/key, **Ubuntu 26.04** (`0.5.4.1`)  
- DNS `A`/`AAAA` for **`sport-lead.ru`** at the registrar (`0.5.6.1`) — domain exists on hosting but is **not** pointed at the VPS yet  
- GitHub deploy key (read) + Actions secrets per `docs/ops/production-17-2-2.md`

## Runbook

Live steps: `docs/ops/vps-canonical-0-5.md` (extends `production-17-2-1` … `17-2-3`).

## Checklist

- [x] `0.5.1` — ADR-032 + this task  
- [x] `0.5.2` — `.cursor/rules/vps-canonical-workflow.mdc` + AGENTS / `dev-server-sessions.mdc`  
- [x] `0.5.3` — owner commit+push WIP (no pycache)  
- [x] `0.5.4.1` — Ubuntu **26.04** host live: Docker CE `29.7.2` + compose `v5.5.0`, git, UFW `22/80/443`, TZ `Europe/Moscow` (no native app runtime)  
- [ ] `0.5.4` — clone + `.env.production` on that host  
- [x] `0.5.5` — `compose.prod.yaml` Postgres `127.0.0.1:5432` + `./storage` bind  
- [ ] `0.5.6.1` — registrar DNS `sport-lead.ru` → VPS; only public origin `https://sport-lead.ru`  
- [ ] `0.5.6` — first `up --build`, TLS, healthz on sport-lead.ru  
- [ ] `0.5.7` — local dump → `prod-restore-db` on VPS  
- [ ] `0.5.8` — GitHub environment `production` + first `workflow_dispatch`  
- [x] `0.5.9` — `scripts/vps-db-tunnel.ps1` + `.env.tunnel.example` port 5433  
- [x] `0.5.10` — `scripts/sync-storage-from-vps.ps1` (VPS disk SoT)  
- [ ] `0.5.11` — cron `prod-backup-db.sh` on VPS  
- [ ] `0.5.12` — owner smoke `https://sport-lead.ru/login` + docs checkpoint  

## Out of scope

Vault; auto-deploy on every push; public Postgres; Tailscale as required layer; inventory UI `12.4.1.5` in this slice.
