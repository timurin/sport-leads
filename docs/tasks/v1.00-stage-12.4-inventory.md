# Task — v1.00 Stage 12.4 Inventory recount

**Roadmap:** `12.4.1.1`–`12.4.1.6` (`SL-ROADMAP-v1.00`)  
**Date:** `2026-08-25`  
**Contract code:** `SL-STOCK-INVENTORY-v1`  
**ADR:** ADR-019 amend `2026-08-25`  
**Depends on:** ADR-012 / 019; ledger MVP `12.2`; movements UI `12.3.3`

## Queue

Stage **7** closed `2026-08-25`. Stage **12.4** closed `2026-08-26` (owner visual `12.4.1.6`). Transfers `12.5` and bins/lots are not this slice.

## SoT

| Entity | Owns | Does not own |
|--------|------|----------------|
| **StockDocument** `inventory` | Recount header (warehouse, draft/posted/cancelled) | Remainder on nomenclature |
| **stock_inventory_lines** | Book snapshot + counted qty | Posted balance |
| **StockLedgerLine** | Signed delta after post (`delta ≠ 0`) | Book/counted display |
| **GET /stock/balances** | Projection of posted ledger | Inventory draft |

## Domain (`12.4.1.1`)

- One warehouse per document. Type `inventory`. Number `STK-*` (existing sequence).
- Lines: unique nomenclature; `book_qty` from posted ledger at fill/refresh; `counted_qty` ≥ 0; `delta = counted − book`.
- Post writes ledger only for non-zero delta (sign = delta). All-zero post allowed. Posted = immutable.
- Draft may refresh book from current posted balance (counted kept).
- Host: `/warehouse/movements`. No Documents module. No bins/lots. No transfer/reserve here.
- MVP does not block shortage below zero (same as `issue`).

## Out of scope (`12.4.1.1`)

SQLAlchemy / Alembic / API / UI. `12.5` transfer. Reserves. 1C. DS-SHELL visual edits.

## Checklist

- [x] `12.4.1.1` — contract (ADR-019 amend)
- [x] `12.4.1.2` — SQLAlchemy + Alembic (`f5a6b7c8d901`)
- [x] `12.4.1.3` — snapshot / counted / post service (`stock_inventory.py`)
- [x] `12.4.1.4` — API + backend tests (`POST /stock/inventory`)
- [x] `12.4.1.5` — UI movements list + card
- [x] `12.4.1.6` — owner visual — `v1.00` `2026-08-26`; list + create drawer + card; thead `tr` hydration fix
