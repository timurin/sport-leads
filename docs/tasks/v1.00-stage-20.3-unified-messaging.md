# Task — 20.3 Unified internal messaging (CRM lead ↔ Stage 19)

**Roadmap:** `20.3.1`–`20.3.3` (`SL-ROADMAP-v1.00`)  
**Date:** `2026-08-05`  
**Contract:** `ADR-027` (`SL` amend of ADR-026)

## 20.3.1 Contract (closed)

**Code:** ADR-027 — `docs/architecture/decisions/ADR-027-unified-internal-messaging-lead-order.md`

### Decision summary

| Topic | Choice |
|-------|--------|
| External CRM channels | Stay on `LeadMessage` / `1.4.3` |
| Staff internal chat | Platform SoT = `Collaboration*` (Stage 19 domain) |
| Shared UI | Yes — reuse / extract order collaboration chrome |
| Shared thread | Yes — extend thread anchor XOR `sales_order_id` \| `lead_id` |
| Convert merge | No auto-merge MVP; separate threads |
| `LeadMessage` `internal` | Stop new writes after `20.3.2`; legacy read OK |

### Forbidden

- Writing staff chat into external `LeadMessage` channels as SoT
- Merging external inbox into collaboration tables
- Silent demo substitution for collaboration API
- Changing Stage 19 order/TC behaviour except shared shell extraction

## 20.3.2 Implement checklist — closed `2026-08-05`

- [x] Migration: nullable `sales_order_id`, add `lead_id`, XOR CHECK + uniques; microtask/notification mirror — `l5m6n7o8p901`
- [x] API: `/leads/{id}/collaboration/...` (+ services)
- [x] Shared FE shell (`OrderCollaborationPanel` leadId|orderId); wire lead page; order/TC hosts unchanged
- [x] Stop routing new lead «internal» sends through `LeadMessage` (channel removed from CRM panel)
- [x] Focused BE tests: `test_collaboration_lead_20_3_2.py` + Stage 19 suite

## 20.3.3 Docs + regression — closed `2026-08-05`

- [x] Amend `order-card-field-links.md` + ADR-026 pointer to ADR-027
- [x] Regression: lead + order collaboration paths green

## Related

- ADR-026, ADR-001, Stage `0.4` (order without lead)
- `20.4.4` order panel appearance parity
