# Task — Stage 22 Design v1.0

**Roadmap:** `22.0`–`22.9` (`SL-ROADMAP-v1.00`)  
**Date:** `2026-08-05`  
**Status:** Through Dashboard live OK `22.8.4`; shell contract `22.9.2`; live FE `22.9.3` open  
**Process SoT:** `docs/design/design-v1-process.md` (`SL-DESIGN-V1-PROCESS-v1`)

## Goal

Migrate owner-approved Soft UI HTML etalons into live platform pages without reopening Stage `20` data contracts. Soft UI **shell** (left + top) only after Stage `22.9` contract + FE (`22.9.2`–`22.9.3`).

## Approved etalons (FE path)

| Screen | HTML | Live route | Host components |
|--------|------|------------|-----------------|
| Lead | `docs/design/sales/lead-card-reference-v1.html` | `/sales/leads/[id]` | `lead-page.tsx` |
| Sales order | `docs/design/sales/order-card-reference-v1.html` | `/sales/orders/[id]` | `sales-order-page.tsx` |
| Production | `docs/design/production/orders-workspace-reference-v1.html` | `/production/orders` | `production-orders-workspace.tsx` |
| Warehouse | `docs/design/warehouse/stock-workspace-reference-v1.html` | `/warehouse/stock` | `warehouse-nomenclature-workspace.tsx` |
| Purchases | `docs/design/purchases/hub-reference-v1.html` | `/purchases` | `purchases-hub-workspace.tsx` |
| Settings | `docs/design/settings/hub-reference-v1.html` | `/settings` | `settings/page.tsx` |
| Dashboard | `docs/design/dashboard/home-reference-v1.html` | `/dashboard` | `home-workspace.tsx` |
| Soft UI shell | `docs/design/shared/shell-reference-v1.html` | `DS-SHELL-01/02` | `app-sidebar.tsx` / `top-navigation.tsx` (FE `22.9.3`) |

Shared: `docs/design/shared/preview.css`, `shell.js`, `demo-data.js`, `preview.js`, `docs/design/index.html`.

## Shell gate (`22.9`)

1. Owner HTML visual OK on shell etalon — **closed** `22.9.1` (`2026-08-22`).
2. Contract vs `DS-SHELL-01` / `DS-SHELL-02` + `navigation.ts` — **closed** `22.9.2` (`SL-DESIGN-V1-SHELL-v1`).
3. Only then FE migrate `app-sidebar.tsx` / `top-navigation.tsx` — **open** `22.9.3`.

## Microtask checklist (high level)

- `22.0`–`22.2` — process + Lead/Order live owner visual OK
- `22.3` — Sales boards TBD (no etalon yet)
- `22.4` — Production live owner visual OK `22.4.4`
- `22.5` — Warehouse live owner visual OK `22.5.4`
- `22.6` — Purchases live owner visual OK `22.6.4`
- `22.7` — Settings live owner visual OK `22.7.4`
- `22.8` — Dashboard live owner visual OK `22.8.4`
- `22.9` — HTML + contract OK; live FE `22.9.3` open

## `SL-DESIGN-V1-PROD-v1` (`22.4.2`)

**Etalon:** `docs/design/production/orders-workspace-reference-v1.html`  
**Primary host:** `/production/orders` → `production-orders-workspace.tsx` (inside `PageLayout`).  
**Related live routes (links only, not restyled in `22.4.3`):** `/production/kanban`, `/production/tech-cards`, `/production/stages/[stageCode]`.  
**No host:** `/production` (nav «Дашборд») has no `page.tsx` — do **not** invent a new dashboard route in `22.4`.

### Chrome

- Wrap workspace with `.sl-design-v1` + Soft UI panels (`.sl-soft-panel` / existing portal cards).
- Flat secondary buttons; primary CTA stays `Button`/`IconButton` portal variants.
- Header: title «Заказы в производстве» + count of loaded POs; actions = link to existing Kanban + existing create drawer. Do **not** add «План смены» (no live route).
- **Shell:** do not change `app-sidebar.tsx` / `top-navigation.tsx` (`22.9`).

### KPI strip (existing fields only)

Etalon shows «критичный срок» / «готовы к отгрузке» / «загрузка цеха». List DTO has **no** due date, shop load, or FG-ready flag.

**Allowed KPIs** — counts from already-fetched `ProductionOrderListItem.status`:

| Label | Source |
|-------|--------|
| В работе | `status === in_progress` |
| Черновик | `status === draft` |
| Завершён | `status === completed` |
| Отменён | `status === cancelled` |

No new API. No fake «нет данных» placeholders for missing etalon KPIs.

### List

Keep current columns (number, sales order, status, batches). Soft-UI the table frame only. Filters stay client-side on the loaded list (list-page rules).

### Shop hubs

Static links from `SHOP_STAGE_MODULES` (`shop-stage-modules.ts`) → `/production/stages/{code}`. No per-shop count API in `22.4` (etalon counts are demo).

### Tech-card quick access

Reuse the technical-card list **already fetched** on the orders page for the create drawer. Show up to 4 slim rows (`id`, `number`, `status`) linking to `/production/tech-cards/[id]`. No extra fetch. No N+1.

### Out of scope

- New business fields / production APIs
- Stage `20` / `3.5` / `11.*` data contracts
- Live DS-SHELL
- Sales boards (`22.3`)
- PO detail / tech-card document / shop kanban restyle (later items if needed)

## `SL-DESIGN-V1-WH-v1` (`22.5.2`)

**Etalon:** `docs/design/warehouse/stock-workspace-reference-v1.html`  
**Primary host:** `/warehouse/stock` → `warehouse-nomenclature-workspace.tsx` (PT-04 tree + list + inspector).  
**Related live routes (links only):** `/warehouse/movements`.  
**No host:** `/warehouse/inventory` (nav exists, no `page.tsx`); `/warehouse` dashboard has no page.

### Chrome

- Wrap workspace with `.sl-design-v1` + header Soft UI panel.
- Keep existing category tree, filters, create/import/export, inspector, остаток column (`12.2.3`).
- Header: «Остатки номенклатуры» + count of items with **positive ledger qty** (`countItemsWithPositiveStock`).
- Primary action = link to existing `/warehouse/movements` (etalon «Оформить приход» has no create-receipt route on this page).
- **Shell:** do not change `DS-SHELL-01/02`.

### Do not invent

| Etalon | Live |
|--------|------|
| Badge «ниже min» | No min/reorder field on nomenclature/stock DTO — omit |
| Tree «Склады» | Warehouse list is Settings catalog, not this tree — keep category tree only |
| Recent movements embed | Separate page `/warehouse/movements`; extra list fetch on stock page forbidden (list-page rules) |
| Inventory draft panel | No inventory workspace — omit «Инвентаризация» button |

### Out of scope

- New stock APIs / min qty / inventory module
- Restyle movements document card (`22.5` host is stock only)
- Live DS-SHELL

## `SL-DESIGN-V1-PUR-v1` (`22.6.2`)

**Etalon:** `docs/design/purchases/hub-reference-v1.html`  
**Primary host:** `/purchases` → `purchases-hub-workspace.tsx`.  
**Nav stubs (same hub, avoid 404):** `/purchases/orders`, `/purchases/suppliers`.  
**Live related links:** `/warehouse/stock`, `/warehouse/movements`.

Stage **13** (suppliers + purchase orders) is **open**. There is no purchases API and no prior live page.

### Chrome

- `.sl-design-v1` hub: header, hub cards, empty PO/supplier panels.
- Honest `0 open` — no Stage 13 documents exist.
- **Shell:** do not change `DS-SHELL-01/02`.

### Do not invent

| Etalon | Live |
|--------|------|
| KPI «1,1 млн ₽» / «ожидают поставки» / «18 поставщиков» | No DTO — omit amounts and fake counts |
| Demo PO-S-2026-* rows | Forbidden (demo substitution) |
| «Новый заказ» CTA | No create API — omit |
| Supplier cards with ИНН | No supplier API — empty + Stage 13 note |

### Out of scope

- Stage 13 domain / API / persistence
- Live DS-SHELL

## `SL-DESIGN-V1-SET-v1` (`22.7.2`)

**Etalon:** `docs/design/settings/hub-reference-v1.html`  
**Primary host:** `/settings` → `settings/page.tsx` (existing catalog/org/users hub).  
**Live CTA:** `/settings/users` (Stage 21).

### Chrome

- Wrap hub with `.sl-design-v1` + Soft UI panels; keep **all existing** group/link cards (do not drop live catalogs to match the shorter etalon).
- Header «Настройки платформы»; primary action = Пользователи.
- **Shell:** do not change `DS-SHELL-01/02`.

### Do not invent

| Etalon | Live |
|--------|------|
| Meta «орг. Sport-Lead Demo» | No org name fetch on this page — omit |
| «Аудит» header button | No dedicated audit-journal route on settings hub — omit (size-grid journal stays on its own page) |
| New `/settings/catalogs/contractors` page | Link already on hub; page may still be missing — do not create a catalog in `22.7` |

### Out of scope

- Restyle every catalog list/card (hub only)
- Live DS-SHELL

## `SL-DESIGN-V1-DASH-v1` (`22.8.2`)

**Etalon:** `docs/design/dashboard/home-reference-v1.html`  
**Primary host:** `/dashboard` → `home-workspace.tsx`.  
**Existing live analytics (do not duplicate here):** `/sales/dashboard` (`1.1.5`).

Home currently has **no** platform-wide KPI/activity/task API. Etalon numbers and feed are demo.

### Chrome

- `.sl-design-v1` header + shortcut cards to live routes: `/sales/leads`, `/production/orders`, `/warehouse/stock`, `/purchases`.
- Header actions: `/sales/dashboard` (live sales KPIs) + `/sales/leads`.
- **Shell:** do not change `DS-SHELL-01/02`.
- Do **not** create `/dashboard/activity` or `/dashboard/analytics` pages in `22.8` (nav stubs remain out of scope).

### Do not invent

| Etalon | Live |
|--------|------|
| KPI 48 / 23 / 11 / 7 and money | No home DTO — omit |
| Chart stub with fake series | Omit (Recharts later needs a real series contract) |
| Activity rows (lead #1042, SO-142, M-882) | Demo — omit |
| «Задачи на сегодня» demo | Use `/sales/tasks` later; do not embed fake WorkTask rows |
| «Новый лид» create CTA | No create query on home — link to `/sales/leads` |

### Out of scope

- New dashboard aggregate API
- Live DS-SHELL (closed in `22.8`; shell is `22.9`)

## `SL-DESIGN-V1-SHELL-v1` (`22.9.2`)

**Etalon:** `docs/design/shared/shell-reference-v1.html` + `preview.css` (rail/topbar) + `shell.js`  
**Hosts:** `frontend/components/navigation/app-sidebar.tsx` (`DS-SHELL-01`), `frontend/components/navigation/top-navigation.tsx` (`DS-SHELL-02`)  
**Nav SoT:** `frontend/lib/navigation.ts` (`appSections` + `topNavigation`)  
**Current contracts:** `docs/design-system/shell-contracts.md` — stay in force for **behavior**. Visual tokens in that file update in `22.9.4` after live owner visual.

`22.9.3` is the only microtask allowed to edit the two host files. Do not restyle shell from any `22.1`–`22.8` page task.

### Chrome to migrate (`22.9.3`)

**Rail**
- Soft UI card: white/blur, radius, light border — labeled rows (icon + title) in expanded mode.
- Active row = primary-soft fill (not icon-only circle).
- Expanded width **220px** (etalon). Compact width **72px** unchanged.
- Brand block stays at top; text/logo from platform settings `18.1.2` (not hardcoded preview «Sport-Lead»).
- Keep existing Lucide **section** icons from `app-sidebar.tsx` (do not replace with letter abbreviations from `shell.js`).

**Topbar**
- Soft UI card chrome (radius / border / blur) around the existing 52/56px height band.
- Search shows a visible field chrome (etalon pill). **Behavior** stays live: expand, autofocus, Escape, hints from `navigation.ts`, no fake API hits.
- Section items are pill tabs sourced from the **current** section `topNavigation`. Grouped items keep live **dropdowns** (preview `TOP_NAV` is flat only because the HTML stub has no groups).
- Keep global Create CTA.

### Preserve (do not drop)

- All live `appSections` (Finance / Analytics exist in platform; preview rail omits them).
- Compact / expanded / hidden + `localStorage` key `sport-lead-sidebar-mode`.
- Force-compact ≤1299 px; rail hidden `max-md`; mobile compact menu = `appSections` + current `topNavigation`.
- Real session profile in the rail footer (not preview «А. Козлов»).
- Topbar vs Page Toolbar split; no second global topbar on pages.
- No hardcoded routes inside the two host files.

### Do not invent from the etalon

| Etalon | Live |
|--------|------|
| Preview-only 6 sections | Keep full `appSections` |
| Design index / «Preview Soft UI» footer | Omit |
| Avatar «А. Козлов» | Real user |
| Notifications «N» | No notifications API in `22.9` — omit |
| Disabled search | Keep live search |
| Page title in topbar (`topbar-meta`) | Omit — product removed section title (`2026-07-21`) |
| Stub top links (activity / analytics / inventory pages) | Do not create routes |

### Out of scope

- New APIs, new nav sections, Stage `20` data contracts
- Sales boards (`22.3`)

## Open next

1. FE Soft UI shell `22.9.3` per `SL-DESIGN-V1-SHELL-v1` (do not start until this contract is the agreed gate — now closed).
2. Then owner live visual `22.9.4` + update `docs/design-system/shell-contracts.md`.
3. Sales boards remain TBD `22.3.1`.
