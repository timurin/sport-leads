# Task — v1.00 Stage 26 Owner findings (bugs / cosmetics / UX)

**Roadmap:** `26.0.1`–`26.9.3` + `26.3.5`–`26.3.13` closed (`SL-ROADMAP-v1.00`)  
**Date:** `2026-08-25`  
**Contract code:** `SL-OWNER-FINDINGS-v1`  
**Canonical:** `docs/roadmap/roadmap-v1.00.md` + HTML twin `docs/erp/status/roadmap-v1.00.html`

## Queue

Living backlog. **Do not auto-start** `26.*` except when the owner names a code (or asks to execute the attached Stage 26 plan). Current v1.00 slice is Stage **12.5** (`12.5.1`).

Do **not** re-open closed `20.*` / `22.*` / `3.5.*` / `19.*`. Do not interrupt `12.5`. Shell `DS-SHELL-01` / `DS-SHELL-02` unchanged except when the owner names `26.9.*`.

## How to add the next finding

1. Owner pastes the finding in chat (or names the screen + defect).
2. Agent adds the next `26.N.M` under the matching block (or a new `26.N` if the surface is new).
3. Same iteration: Markdown checkbox **and** HTML twin `{ code, done }` — atomic. No «HTML later».
4. Do **not** start code until the owner names that code (unless the owner approved a plan that names the codes).
5. Close only after applicable checks + owner visual when the item is UI.

## Seed (`2026-08-25`) + append (`2026-08-26`)

| Code | Surface | Status |
|------|---------|--------|
| `26.0.1` | Process / contract | **closed** this docs pass |
| `26.1.1` | Sales order card — delete line with TC | **closed** `2026-08-26` (HTTP 409 + alert; no cascade) |
| `26.1.2` | Sales order card — live payment scale | **closed** `2026-08-26` |
| `26.1.3` | Sales order + lead — hide Количество | **closed** `2026-08-26` (no DROP) |
| `26.1.4` | Sales order — Дата отгрузки | **closed** `2026-08-26` (lead: desired_date ≠ planned_order_date) |
| `26.1.5` | Sales order — source Select | **closed** `2026-08-26` |
| `26.1.6` | Sales order card — aside Переписка | **closed** `2026-08-26` (filter Коммуникация) |
| `26.2.1` | Order tech-cards panel — readiness bar | **closed** `2026-08-26` |
| `26.2.2` | Order tech-cards panel — mini grid + stage strips | **closed** `2026-08-26` |
| `26.3.1` | Tech-card document — ADR-026 right rail | **closed** `2026-08-26` |
| `26.3.2` | Tech-card document — three blocks in a row | **closed** `2026-08-26` |
| `26.3.3` | Tech-card document — horizontal manager route | **closed** `2026-08-26` |
| `26.3.4` | Tech-card document — Персонализация row 2 | **closed** `2026-08-26` |
| `26.3.5` | Tech-card — order-data edit chrome | **closed** `2026-08-27` (owner visual `/production/tech-cards/6`) |
| `26.3.6` | Tech-card — responsible manager picker | **closed** `2026-08-28` (owner visual `/production/tech-cards/6`) |
| `26.3.7` | Tech-card — client typeahead + create modal | **closed** `2026-08-28` (owner visual `/production/tech-cards/6`) |
| `26.3.8` | Tech-card — due-date calendar | **closed** `2026-08-28` (owner visual `/production/tech-cards/6`) |
| `26.3.9` | Tech-card — model + assembly edit | **closed** `2026-08-28` (owner visual `/production/tech-cards/6`) |
| `26.3.10` | Tech-card — ops/assembly/materials full-width stack | **closed** `2026-08-28` (owner visual `/production/tech-cards/6`) |
| `26.3.11` | Tech-card — toolbar product name | **closed** `2026-08-28` (owner visual `/production/tech-cards/6`) |
| `26.3.12` | Tech-card — restore mockup add/delete images | **closed** `2026-08-28` (owner confirmed OK) |
| `26.3.13` | Tech-card — restore internal collab (incl. contour B; `28.5.4`) | **closed** `2026-08-28` (owner visual `/production/tech-cards/6`) |
| `26.4.1`–`26.4.3` | Nomenclature card models + variants | **closed** `2026-08-26` (whitelist-only models; card «Варианты» removed) |
| `26.5.1`–`26.5.2` | Lead card layout A | **closed** `2026-08-26` (owner visual) |
| `26.5.3`–`26.5.4` | Slider 90% + Interest/Delivery/Metrics + extra fields RBAC | **closed** `2026-08-26` |
| `26.6.1`–`26.6.8` | Unified History tree + 50% event modal | **closed** `2026-08-26` (owner visual `26.6.8`) |
| `26.7.1` | Product model card — category Select (`folder_id`) | **closed** `2026-08-26` |
| `26.8.1` | Common system — sport-lead.ru image upload | **closed** `2026-08-26` |
| `26.8.2` | Common system — left sidebar 10% width | **closed** `2026-08-26` |
| `26.9.1` | Platform nav — three sidebar contours | **closed** `2026-08-27` |
| `26.9.2` | Settings gear + `/settings` slider | **closed** `2026-08-27` |
| `26.9.3` | Owner visual nav + settings slider | **closed** `2026-08-27` (owner visual) |

Filter **Документ** hides the tech-cards panel — out of 26.2 unless a new finding.

## Evidence — `26.1.1`

- Backend: `delete_sales_order_item` raises `SalesOrderItemError` if a `TechnicalCard` exists; API `DELETE /orders/{id}/items/{item_id}` → **409**; no cascade.
- Frontend: `sales-order-items-unf-demo.tsx` `deleteSelected` loads tech-card rows, `window.alert` with number + status, does not delete.
- Tests: `backend/tests/test_order_item_delete_26_1_1.py`; `frontend/lib/sales/order-item-tech-card-guard.test.mjs`.

## Layout for `26.3.1` (owner-agreed sketch)

xl+: sticky right rail for ADR-026 collaboration; main column:

1. Макет | Данные по заказу | Модель и маршрут  
2. Персонализация (rename from Поштучно)  
3. Операции / объёмы | Схема сборки | Материалы  
4. Маршрут горизонтально (manager document; shop `?stage=` chips stay)

Tablet: collapsible «Переписка» under row 1. Mobile: accordion after header. Shop-floor body: do not mix chat into mockup.

## `26.4` — Nomenclature `/settings/catalogs/nomenclature/[id]`

- Card list of «Модели изделий» = whitelist `links` only. `+` opens picker of remaining active models of the selected Вид изделия. Backend already does not auto-insert (changing вид clears links).
- Owner `2026-08-26`: «Варианты» on the nomenclature card has no business or technical need — removed (`26.4.3`). Variant API + order snapshots stay. ADR-006 still distinguishes variant ≠ product model.

## Evidence — `26.4.1` / `26.4.2` / `26.4.3`

- Models: card list is whitelist `links` only; `+` picker = active models of selected Вид изделия minus linked ids. Host `nomenclature-available-models-block.tsx`. Tests: `frontend/lib/nomenclature-available-models.test.mjs`; pytest `test_nomenclature_available_models.py` (no auto-insert).
- `26.4.2` generate/create UI shipped then superseded: `26.4.3` removes `NomenclatureVariantsBlock` and card fetch of variants. Test: `frontend/lib/nomenclature-variants-26-4-3.test.mjs`. Generate API remains for orders.

## Evidence — `26.1.3`

- Order «Основные сведения»: quantity field hidden in edit + view (`order-client-need-details.tsx`); save still sends `draft.quantity` / existing `quantityValue` (no DROP).
- Lead: `hideQuantity` default true; «Количество изделий» omitted; product summary does not append `шт`; persist `estimatedQuantity` unchanged.
- Test: `frontend/lib/sales/order-hide-quantity-26-1-3.test.mjs`.

## Evidence — `26.1.4`

- Order «Основные сведения»: label «Желаемая дата» → «Дата отгрузки» (view + edit). Payload still `desired_date` / `desiredDate`.
- Lead: `desired_date` / `desiredReadyDate` = **Дата отгрузки**. `planned_order_date` = **Дата входа в производство** (not a synonym). PATCH includes `desired_date`.
- Test: `frontend/lib/sales/order-ship-date-26-1-4.test.mjs`.

## Evidence — `26.1.5`

- Order source: same Select as lead create (`leadCreateSourceOptions`). Empty = «Не указано». Filled known codes kept; unknown filled values stay as extra option.
- Test: `frontend/lib/sales/order-source-select-26-1-5.test.mjs`.

## Evidence — `26.1.6`

- Aside tabs: Финансы / Основные сведения / Заметки / Задачи. No «Переписка».
- Filter **Коммуникация** shows `OrderCollaborationPanel` in the main column (`visibility.communication`).
- Tech-card document still hosts ADR-026 `OrderCollaborationPanel`.
- Tests: `frontend/lib/sales/order-aside-no-chat-26-1-6.test.mjs`; `order-card-view-mode.test.mjs`.

## Evidence — `26.2.1`

- Removed summary block «Готовность производства» (Eligible / counts).
- Gradient bar under SectionCard title «Технические карты»: 0 = ТК не запущены, 100 = этап «Готовы к отгрузке» закрыт (не `completedCount/eligibleCount`).
- Tests: `frontend/lib/sales/order-tech-cards.test.mjs`; `order-tech-cards-readiness-26-2-1.test.mjs`.

## Evidence — `26.2.2`

- Mini-cards: `grid-cols-1` / `lg:grid-cols-3`; title `номер — наименование`; Open is an icon at the title right (`aria-label="Открыть"`).
- Stage strips from `stage_results`: done / active / upcoming.
- `GET /orders/{id}/technical-cards` → `TechnicalCardListRead` (slim + `stage_results`; no `unit_lines` / composition / media).
- Tests: `backend/tests/test_order_tech_cards_list_26_2_2.py`; `frontend/lib/sales/order-tech-cards-grid-26-2-2.test.mjs`.

## Evidence — `26.3.1`

- xl+: sticky right rail `Сотрудничество по заказу` (one `OrderCollaborationPanel`).
- Tablet: collapsible «Переписка» under row 1 (Макет | Данные | Модель).
- Mobile: accordion «Переписка» after header.
- Shop `?stage=`: chat not inside `TechCardShopFloorBody` mockup; same collapse/rail beside the shop body.
- Tests: `frontend/lib/production/tech-card-collab-rail-26-3-1.test.mjs`.

## Evidence — `26.3.2`

- Manager document: `data-tech-card-doc-row3` — «Операции / объёмы», «Схема сборки изделия», «Состав материалов» in `xl:grid-cols-3` (stack below xl).
- «Маршрут / участки» stays full-width below (`26.3.3`). Unit lines moved/renamed in `26.3.4`.
- Shop `?stage=` unchanged.
- Tests: `frontend/lib/production/tech-card-blocks-row-26-3-2.test.mjs`.

## Evidence — `26.3.3`

- Manager «Маршрут / участки»: `ol[data-tech-card-manager-route]` `flex-wrap` of stage cards (actions + equipment stay).
- Shop `tech-card-shop-floor-body.tsx` overview chips unchanged (`ol.flex.flex-wrap`).
- Tests: `frontend/lib/production/tech-card-manager-route-26-3-3.test.mjs`.

## Evidence — `26.3.4`

- Manager: title «Персонализация»; `data-tech-card-doc-row2` after Макет | Данные | Модель, before ops/assembly/materials.
- Shop `?stage=` still «Поштучно». Print template still «Поштучно».

## Evidence — `26.3.5` (closed `2026-08-27`, owner visual OK)

Manager document only. Shop `?stage=` unchanged.

- Block «Данные по заказу»: `IconButton` Pencil (view) / X + Save (edit) via `data-tech-card-order-data-chrome`.
- Edit chrome shipped; persist widgets: `26.3.6` manager, `26.3.7` client, `26.3.8` due date.
- Host: `frontend/components/production/tech-card-order-data-card.tsx`.
- Test: `frontend/lib/production/tech-card-order-data-edit-26-3-5.test.mjs`.
- Visual: `/production/tech-cards/6` (local `:3001`) — owner OK.

## Evidence — `26.3.6` (closed `2026-08-28`, owner visual OK)

Manager document only. Shop `?stage=` unchanged.

- Responsible is `PlatformUser` (not `SalesUser`). Default display = assigned user, else creator, else legacy sales-order responsible.
- Combobox of active Users with `technical_cards.create` (`data-tech-card-responsible-combobox`). Save persists `responsible_platform_user_id` (empty → creator).
- Alembic `n3o4p5q6r789`; `GET /technical-cards/responsible-candidates`; `PATCH /technical-cards/{id}/responsible`.
- Tests: `backend/tests/test_tech_card_responsible_26_3_6.py`; `frontend/lib/production/tech-card-responsible-26-3-6.test.mjs`.
- Visual: `/production/tech-cards/6` (local `:3001`) — owner OK.

## Evidence — `26.3.7` (closed `2026-08-28`, owner visual OK)

Manager document only. Shop `?stage=` unchanged.

- Typeahead from `GET /clients?q=` (`data-tech-card-client-combobox`). «Создать клиента» opens existing `ClientCreateDrawer` (no redirect to `/sales/clients`).
- Persist: contour A → `SalesOrder.client_id` (required); contour B → `TechnicalCardOrderGroup.client_id` (nullable). Alembic `o4p5q6r7s890`; `PATCH /technical-cards/{id}/client`.
- Tests: `backend/tests/test_tech_card_client_26_3_7.py`; `frontend/lib/production/tech-card-client-26-3-7.test.mjs`.
- Visual: `/production/tech-cards/6` (local `:3001`) — owner OK.

## Evidence — `26.3.8` (closed `2026-08-28`, owner visual OK)

Manager document only. Shop `?stage=` unchanged.

- Edit mode: calendar with day grid + month/year selects (`data-tech-card-due-date-calendar`). View mode stays formatted text.
- Persist: contour A → `SalesOrder.desired_date` (nullable); contour B → `TechnicalCardOrderGroup.desired_date` (required). `PATCH /technical-cards/{id}/desired-date`. No new Alembic (dates already exist).
- Tests: `backend/tests/test_tech_card_desired_date_26_3_8.py`; `frontend/lib/production/tech-card-due-date-26-3-8.test.mjs`.
- Visual: `/production/tech-cards/6` (local `:3001`) — owner OK.

## Evidence — `26.3.9` (closed `2026-08-28`, owner visual OK)

Manager document only. Shop `?stage=` unchanged.

- Block «Модель и маршрут»: Pencil / X / Save (`data-tech-card-model-route-chrome`). Model typeahead from `GET /product-models` (`data-tech-card-model-combobox`). Assembly Select of selected model (`data-tech-card-assembly-select`). Routing Select kept immediate-apply (`data-tech-card-routing-select`).
- Persist: `PATCH /technical-cards/{id}/model-assembly` snapshots on the card; contour A also updates `SalesOrderItem` model/assembly (routing unchanged). Sewing lines refresh from item snapshots or live assembly.
- Tests: `backend/tests/test_tech_card_model_assembly_26_3_9.py`; `frontend/lib/production/tech-card-model-route-26-3-9.test.mjs`.
- Visual: `/production/tech-cards/6` (local `:3001`) — owner OK.

## Evidence — `26.3.10` (closed `2026-08-28`)

Manager document only. Shop `?stage=` unchanged. Do not restore 3-col (`26.3.2` layout).

- Owner: «в ряд» = full-width stack like «Маршрут / участки» and «История», not three columns.
- Row 3 (`data-tech-card-doc-row3`): `flex flex-col gap-portal-4` (not `grid-cols-3`).
- Tests: `frontend/lib/production/tech-card-blocks-row-26-3-10.test.mjs`; `tech-card-blocks-row-26-3-2.test.mjs` (order of blocks unchanged).
- Visual: `/production/tech-cards/6` (local `:3001`) — owner OK.

## Evidence — `26.3.11` (closed `2026-08-28`)

Manager document only. Shop `?stage=` shows the name, no Pencil.

- EntityHeader `description`: `TechCardProductNameHeader` (`data-tech-card-product-name`). Title stays `card.number`.
- Persist: `PATCH /technical-cards/{id}/nomenclature-name` → snapshot `TechnicalCard.nomenclature_name` (strip; blank → `null`; max 255). Catalog `Nomenclature` not updated.
- Tests: `backend/tests/test_tech_card_product_name_26_3_11.py`; `frontend/lib/production/tech-card-product-name-26-3-11.test.mjs`.
- Visual: `/production/tech-cards/6` (local `:3001`) — owner OK.

## Evidence — `26.3.12` (closed `2026-08-28`)

Manager document only. No code delta — add/delete already live in `tech-card-media-carousel.tsx` (hover/focus icons; max 3). Shop `readOnly={!mediaEditable}`.

- Tests: `frontend/lib/production/tech-card-mockup-media-26-3-12.test.mjs`.
- Visual: `/production/tech-cards/6` — owner confirmed OK.

## Evidence — `26.3.13` (closed `2026-08-28`)

Manager document. Shop `?stage=` keeps chat beside the shop body (not inside mockup). Owner visual OK.

- Contour A: `OrderCollaborationPanel` `orderId` + `technicalCardId` (ADR-026 rail / accordion «Переписка»).
- Contour B: `standaloneCardId` → `GET/POST /technical-cards/{id}/collaboration/messages` (`28.5.4`).
- Tests: `frontend/lib/production/tech-card-collab-26-3-13.test.mjs` (`26.3.1` / `28.5.4` tests still pass).
- Visual: `/production/tech-cards/6` — xl: right rail «Сотрудничество по заказу»; below xl: accordion «Переписка». Standalone TC: title «Сотрудничество».

## Evidence — `26.5.1` / `26.5.2` (closed `2026-08-26`)

PT-06 header + stage rail stay. Owner visual `/sales/leads/1001` OK.

- Wide main (~65–70%): unified History tree (`26.6`). Internal collab is in the tree, not a separate accordion.
- Narrow left (~30%): contact + interest. Quantity hidden via `26.1.3`.
- 2–3 pills instead of six MetricCard tiles. Do not triplicate phone/email (drop communication-panel `customerSummary`).
- Mobile CompactTabs: Коммуникации | Клиент | Интерес.
- Slider width `92%` from `lg`. Left facts column `minmax(240px, 22rem)`.
- Compact blocks: Клиент; Интерес; Доставка; Метрики. Extra fields: `leads.card_fields.manage`.

## `26.6` — Unified feed + event modal (closed `2026-08-26`)

- Default right column: History-style tree (messages, tasks, notes, internal collab). No feed tabs, no page composer, no tasks kanban, no accordion «Внутренняя переписка».
- Filters: event type + channel. Empty = compact EmptyState.
- Nested modal 50% viewport width over the lead slider: view event; compose (header «Написать») / reply (message + internal). file/system read-only. Task: view + existing create drawer.
- Owner visual `26.6.8` on `/sales/leads/1001` OK `2026-08-26`.
- Shipped: `lead-page.tsx` unified tree; `lead-event-modal.tsx` 50vw; collab merged client-side.
- Shell unchanged. No ADR-026 API change.

## `26.7` — Product model card `/settings/catalogs/product-models/[id]`

Closed `26.7.1` `2026-08-26`. Example: `/settings/catalogs/product-models/104`.

- Category = catalog **folder** (`folder_id`, Stage `6.1.18`). Card Select «Категория»; empty = «Без папки».
- «Вид изделия» and «Маршрут по умолчанию» unchanged.
- PATCH `updateProductModelRequisites` sends `folder_id`.
- Tests: `frontend/lib/product-model-category-26-7-1.test.mjs`; `product-models.test.mjs` (`productModelFolderSelectOptions`).

## `26.8` — Common system bugs

Closed `26.8.1` `2026-08-26`. Owner: image upload fails on `https://sport-lead.ru`. ≠ `0.5.13` S3.

- Browser `img`/`href` for catalog/model/TC/design/logo media → `/api/media/...` (Next GET proxies `SPORT_LEADS_API_URL` with session cookie; allowlisted `/…/content` paths only).
- Helpers: `sameOriginApiMediaUrl` (`productModelCoverUrl`, `nomenclatureMediaUrl`, `technicalCardMediaContentUrl`, `platformMediaUrl`, `designAssetContentAbsoluteUrl`).
- Empty `file.type`: jpeg/png/webp from extension; HEIC still rejected.
- Next `serverActions.bodySizeLimit` **15mb** (base64 of 10mb file + JSON).
- Bytes stay on VPS `storage/` (`0.5.10`). Live `sport-lead.ru` after owner commit/push + deploy.
- Tests: `frontend/lib/api-media-26-8-1.test.mjs`.

### `26.8.2` — Left sidebar 10%

Closed `2026-08-26`. Owner: expanded left menu **10%** of viewport (seed was 6%).

- `--portal-shell-sidebar-expanded: max(220px, 10vw)` on unlayered `flex-basis`/`width` of `[data-app-shell] > aside`. Tailwind `w-[var]` did not set flex-basis. Compact `72px`. Below 2200px viewport `10vw < 220px` so used width stays 220px.
- Compact `72px` unchanged. JSX frame / colors unchanged (`DS-SHELL-01` width token only).
- Contracts: `shell-contracts.md`, `shell-sidebar-standardization.md`, `component-size-tokens.md`.

## `26.9` — Platform nav: sales vs production

**`26.9.1`–`26.9.3` closed `2026-08-27`.** Owner visual OK (expanded + compact; desktop + md; gear slider vs full `/settings`).

**Shipped (`26.9.1`):**

- SoT: `SIDEBAR_CONTOURS` + `groupSectionsByContour` in `frontend/lib/navigation.ts`.
- Expanded rail labels: **Продажи** / **Производство**. Compact: same icon order + dividers.
- **Продажи:** Главная, Продажи, Отчеты, Финансы, Аналитика. **Отчеты** is a top-level section reusing `/sales/reports/funnel|managers|dynamics` (no new pages). Nested `sales-reports` removed from Продажи.
- **Производство:** Производство, Склад, Закупки (routes unchanged).
- Tests: `frontend/lib/navigation.test.mjs`, `shell-soft-ui.test.mjs`.

**Shipped (`26.9.2`):**

- Настройки removed from the left rail (`SIDEBAR_CONTOURS` + leftover skip).
- DS-SHELL-02 gear (`data-settings-topbar-link`) → `/settings`.
- Soft nav intercept `@settingsSlider/(.)settings` + `SettingsCardSlider` (same 92% chrome as lead). Close = `router.back()` or `/dashboard`.
- Nested `/settings/...` stay full pages (URL unchanged). Hard refresh of `/settings` is the hub page (no intercept).
- Tests: `frontend/lib/settings/settings-slider-26-9-2.test.mjs`.

3. **`26.9.3`** — Owner visual (expanded/compact, desktop/md, icon + slider vs full `/settings`). **Closed `2026-08-27`.**

## Out of scope until owner-pull

Cascade-delete of tech cards with order lines. DROP of quantity/source columns. Design v1.0 HTML etalons (Stage 22 closed). `DATABASE_URL` / tunnel `:5433`.
