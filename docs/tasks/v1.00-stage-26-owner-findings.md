# Task — v1.00 Stage 26 Owner findings (bugs / cosmetics / UX)

**Roadmap:** `26.0.1`–`26.5.2` (`SL-ROADMAP-v1.00`)  
**Date:** `2026-08-25`  
**Contract code:** `SL-OWNER-FINDINGS-v1`  
**Canonical:** `docs/roadmap/roadmap-v1.00.md` + HTML twin `docs/erp/status/roadmap-v1.00.html`

## Queue

Living backlog. **Do not auto-start** `26.*` except when the owner names a code (or asks to execute the attached Stage 26 plan). Current v1.00 slice is Stage **12.5** (`12.5.1`).

Do **not** re-open closed `20.*` / `22.*` / `3.5.*` / `19.*`. Do not interrupt `12.5`. Shell `DS-SHELL-01` / `DS-SHELL-02` unchanged.

## How to add the next finding

1. Owner pastes the finding in chat (or names the screen + defect).
2. Agent adds the next `26.N.M` under the matching block (or a new `26.N` if the surface is new).
3. Same iteration: Markdown checkbox **and** HTML twin `{ code, done }` — atomic. No «HTML later».
4. Do **not** start code until the owner names that code (unless the owner approved a plan that names the codes).
5. Close only after applicable checks + owner visual when the item is UI.

## Seed (`2026-08-25`) + append (`2026-08-26`)

| Code | Surface | Status |
|------|---------|--------|
| `26.0.1` | Process / contract | **closed** this docs pass |
| `26.1.1` | Sales order card — delete line with TC | **closed** `2026-08-26` (HTTP 409 + alert; no cascade) |
| `26.1.2` | Sales order card — live payment scale | **closed** `2026-08-26` |
| `26.1.3`–`26.1.6` | Sales order card | parked |
| `26.2.1`–`26.2.2` | Order tech-cards panel (`SalesOrderTechCardsPanel`; filters Товары + Тех карты) | parked |
| `26.3.1`–`26.3.4` | Tech-card document | parked |
| `26.4.1`–`26.4.3` | Nomenclature card models + variants | **closed** `2026-08-26` (whitelist-only models; card «Варианты» removed) |
| `26.5.1`–`26.5.2` | Lead card layout A | `26.5.1` shipped pending owner visual `26.5.2` |

Filter **Документ** hides the tech-cards panel — out of 26.2 unless a new finding.

## Evidence — `26.1.1`

- Backend: `delete_sales_order_item` raises `SalesOrderItemError` if a `TechnicalCard` exists; API `DELETE /orders/{id}/items/{item_id}` → **409**; no cascade.
- Frontend: `sales-order-items-unf-demo.tsx` `deleteSelected` loads tech-card rows, `window.alert` with number + status, does not delete.
- Tests: `backend/tests/test_order_item_delete_26_1_1.py`; `frontend/lib/sales/order-item-tech-card-guard.test.mjs`.

## Layout for `26.3.1` (owner-agreed sketch)

xl+: sticky right rail for ADR-026 collaboration; main column:

1. Макет | Данные по заказу | Модель и маршрут  
2. Персонализация (rename from Поштучно)  
3. Операции / объёмы | Схема сборки | Материалы  
4. Маршрут горизонтально (manager document; shop `?stage=` chips stay)

Tablet: collapsible «Переписка» under row 1. Mobile: accordion after header. Shop-floor body: do not mix chat into mockup.

## `26.4` — Nomenclature `/settings/catalogs/nomenclature/[id]`

- Card list of «Модели изделий» = whitelist `links` only. `+` opens picker of remaining active models of the selected Вид изделия. Backend already does not auto-insert (changing вид clears links).
- Owner `2026-08-26`: «Варианты» on the nomenclature card has no business or technical need — removed (`26.4.3`). Variant API + order snapshots stay. ADR-006 still distinguishes variant ≠ product model.

## Evidence — `26.4.1` / `26.4.2` / `26.4.3`

- Models: card list is whitelist `links` only; `+` picker = active models of selected Вид изделия minus linked ids. Host `nomenclature-available-models-block.tsx`. Tests: `frontend/lib/nomenclature-available-models.test.mjs`; pytest `test_nomenclature_available_models.py` (no auto-insert).
- `26.4.2` generate/create UI shipped then superseded: `26.4.3` removes `NomenclatureVariantsBlock` and card fetch of variants. Test: `frontend/lib/nomenclature-variants-26-4-3.test.mjs`. Generate API remains for orders.

## Evidence — `26.5.1` (pending owner visual `26.5.2`)

PT-06 header + stage rail stay.

- Wide main (~65–70%): composer + feed filters (коммуникация / задачи / заметки / история). Internal `OrderCollaborationPanel` stays in this column.
- Narrow left (~30%): contact + interest accordions. Hide «Количество изделий» here without closing parked `26.1.3`.
- 2–3 pills instead of six MetricCard tiles. Do not triplicate phone/email (drop communication-panel `customerSummary`).
- Mobile CompactTabs: Коммуникации | Клиент | Интерес.
- Close `26.5.1` only with owner visual `26.5.2` on `/sales/leads/1001`.
- Owner `2026-08-26` picked layout **A** (facts left, composer+feed right). HTML options: `docs/design/sales/lead-card-layout-options-v1.html` (not dashboard 22.8 etalon). Live: composer above history; no footer action strip.

## Out of scope until owner-pull

Cascade-delete of tech cards with order lines. DROP of quantity/source columns. Design v1.0 HTML etalons (Stage 22 closed). `DATABASE_URL` / tunnel `:5433`.
