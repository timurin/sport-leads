# Task — v1.00 Stage 28 Standalone tech cards + unified numbering

**Roadmap:** `28.0.1`–`28.5.4` (`SL-ROADMAP-v1.00`)  
**Date:** `2026-08-27`  
**Contract code:** `SL-STANDALONE-TC-v1`  
**Canonical:** `docs/roadmap/roadmap-v1.00.md` + HTML twin `docs/erp/status/roadmap-v1.00.html`

## Queue

Owner named Stage 28 (`2026-08-27`). **`28.0.1`–`28.5.4` closed** (`2026-08-27`). Do not auto-start `27.1+` / `26.3.6`+.

Do **not** mix this stage with inventing a second shop-fact SoT, child TCs per unit, or hard-blocking generate when actual TC count ≠ planned count.

## Contract (`SL-STANDALONE-TC-v1`)

### Dual contours

| Contour | Entry | Rule |
|---------|--------|------|
| **A** | `SalesOrder` → `SalesOrderItem` → PRODUCT | Generate TC from selected nomenclature + item qty → unit lines (ADR-016). Unchanged cardinality: ≤1 TC per eligible item. |
| **B** | Manual create without required `SalesOrder` | Internal production (scrap, gifts, etc.). TC links to `technical_card_order_groups`. |

Both share shop / QR / FG execution on the TechnicalCard. Spec without SalesOrder is **closed** (`28.5.3`). ProductionOrder without SalesOrder is **closed** (`28.5.2`). Collab without SalesOrder is **closed** (`28.5.4`).

### Order group (contour B)

| Field | Rule |
|-------|------|
| `order_number` | Free text; **unique among standalone groups** (may coincide with a real `SalesOrder.number` — no FK) |
| `tech_cards_planned_count` | Manager-entered plan (≥ 1); **no** auto-create of N cards |
| `desired_date` | Manual ship date (static snapshot for B) |

### Planned TC count (both contours)

- On **A**: field on `SalesOrder` (`tech_cards_planned_count`).
- On **B**: field on the order group.
- Meaning: how many tech cards the manager expects for that order/group.
- **Soft progress only** («3 of 5»): create/generate is **not** blocked when actual ≠ planned.

### Numbering

| Layer | Format |
|-------|--------|
| Stored `TechnicalCard.number` | `{orderNo}-{card_seq}` |
| Display / print / UI | `{orderNo}-{card_seq}/{N}` |

`N` = **live** planned count from SalesOrder or group. Changing planned count updates display everywhere without rewriting stored `number`.

Example: order `1310`, planned `5`, first card → store `1310-1`, show `1310-1/5`.

### Contour B create form (MVP)

Required:

1. Nomenclature (`PRODUCT`)
2. Order number (group key)
3. Planned tech-card count
4. Ship date (`desired_date`)
5. Quantity ≥ 1 → create **qty** unit lines immediately

### FK rules (when implementing `28.2.1`)

- `sales_order_id` / `sales_order_item_id` → **nullable**
- Both null **or** both set (no half-link)
- UNIQUE on `sales_order_item_id` only when not null
- Contour B: `order_group_id` set; SalesOrder FKs null

### ADR amend (under `28.0.1`)

- **ADR-016** — second entry path; numbering display; soft planned count; nullable order FKs for B
- **ADR-004** — dual chain into production (A via SalesOrder, B via standalone TC)

Do **not** amend ADR files until `28.0.1` is executed. **Done `2026-08-27`:** ADR-016 §1.1; ADR-004 dual chain.

## Out of MVP / parked

None remaining in Stage 28. `26.3.13` (owner findings restore collab on the manager document) stays parked separately.

## Execution order (when the owner names a code)

1. `28.0.1` — lock ADR amend + this contract
2. `28.1` — SalesOrder planned count + display `/{N}` + soft progress UI
3. `28.2` — group table + nullable FKs + create API
4. `28.3` — FE create drawer + standalone document branch
5. `28.4` — print / shop / FG regression + docs
6. `28.5` — only by explicit owner-pull

## Codes

| Code | Status |
|------|--------|
| `28.0.1` | **closed** `2026-08-27` — contract + ADR-016/004 |
| `28.1.1`–`28.1.6` | **closed** `2026-08-27` — planned count on SalesOrder + display |
| `28.2.1`–`28.2.5` | **closed** `2026-08-27` — standalone group + API |
| `28.3.1`–`28.3.2` | **closed** `2026-08-27` — FE create + document branch |
| `28.3.3` | **closed** `2026-08-27` — owner visual OK |
| `28.4.1`–`28.4.3` | **closed** `2026-08-27` — print / scan / FG / docs |
| `28.5.1` | **closed** `2026-08-27` — B→A `POST /technical-cards/{id}/link-sales-order-item`; owner visual OK |
| `28.5.2` | **closed** `2026-08-27` — PO nullable SO + group XOR; Alembic `k0l1m2n3o456` |
| `28.5.3` | **closed** `2026-08-27` — Spec nullable SO; Alembic `l1m2n3o4p567` |
| `28.5.4` | **closed** `2026-08-27` — collab on standalone TC; Alembic `m2n3o4p5q678` |

## Related

- ADR-016 technical card domain
- ADR-004 production chain + Spec
- ADR-018 ProductionOrder/Batch (`28.5.2` closed: nullable SO + group XOR)
- ADR-026 collaboration (`28.5.4` closed: order-group XOR)
- ADR-030 / Stage 25 QR scan (must stay null-order safe in `28.4.2`)
- Stage `0.4` order without lead ≠ this stage (still requires SalesOrder for TC generate today)
