# Task — v1.00 Stage 7 Specifications (plan+fact report)

**Roadmap:** `7.1.1.1`–`7.2.3.6` (`SL-ROADMAP-v1.00`)  
**Date:** `2026-08-25`  
**Contract code:** `SL-SPEC-DOCUMENT-v1`  
**ADR:** ADR-031 (`docs/architecture/decisions/ADR-031-specification-document.md`)  
**Depends on:** ADR-004 / 014 / 016 / 018; TC composition + stage fact; sewing ledger Stage 24 (read on refresh)

## Queue

Stage **7** closed `2026-08-25` (owner visual `7.2.2.6`). Next slice: Stage **12.4** inventory.

## SoT

| Entity | Owns | Does not own |
|--------|------|----------------|
| **Specification** (this Stage) | Batch plan+fact **report document** + versions | BOM before TC; shop fact; stock postings |
| **TechnicalCard** (ADR-016) | Composition plan/fact, op volumes, stage results | 1C cost report |
| **Sewing work ledger** (ADR-029) | Sewing completed qty for Spec fact refresh | Spec lines |
| **ProductionBatch** (ADR-018) | Parent of Spec (1:1) | Spec field values |
| **Documents registry** (later) | Link index | Spec persistence |

## Domain (`7.1.1`)

### Parent + entities

One `Specification` per `ProductionBatch`. Versions + product / material / operation lines on the version. Number `{batchNumber}-SPEC`. TC must already be in the batch.

### Lifecycle

`draft` (refreshable, one per header) → `approved` (immutable; all linked TCs terminal) → later draft `v{n+1}` then approve supersedes previous. `cancelled` for discarded drafts.

**Not a start gate:** batch / TC do not wait for Spec.

### Materials

TC `line_kind=material` only. Plan = `planned_qty`, fact = `fact_qty`. No accessory type. No substitute table (TC has none). No stock-ledger second SoT.

### Copy/read

Assembly + routing/sewing volumes from **TC snapshots** (order-item already snapshotted). Performers / time from stage results; sewing fact qty from completed ledger. Live model edit does not rewrite `approved` versions.

### Documents

Do **not** build Documents module. Spec lives on the batch; interim list `/production/specifications` in `7.2.2`.

## Out of scope (`7.1.1`)

SQLAlchemy / Alembic / API / UI. 1C adapter. Costing Stage 15. DS-SHELL visual edits. New RBAC codes.

## Feeds

- `7.1.2` DB core  
- `7.2.1` CRUD API  
- `7.2.2` workspace + card  
- `7.2.3` relations + create-from-TC + immutability tests  

## Checklist

- [x] `7.1.1.1` — entities + version lifecycle
- [x] `7.1.1.2` — material / norm / substitute scope
- [x] `7.1.1.3` — copy/read contract
- [x] `7.1.1.4` — Documents registry = link index only
- [x] `7.1.2.1` — SQLAlchemy entities (`Specification*` + TC FK)
- [x] `7.1.2.2` — Alembic `e4f5a6b7c890`
- [x] `7.1.2.3` — schemas + backend regression tests
- [x] `7.2.1.1` — service CRUD
- [x] `7.2.1.2` — endpoints
- [x] `7.2.1.3` — backend API tests
- [x] `7.2.2.1` — FE types + API client
- [x] `7.2.2.2` — list `/production/specifications`
- [x] `7.2.2.3` — PT-07 card (plan+fact; actions, no free-form edit)
- [x] `7.2.2.4` — loading/error
- [x] `7.2.2.5` — FE tests
- [x] `7.2.2.6` — owner visual (list + card + batch link)
- [x] `7.2.3.1` — relation fields (in `e4f5a6b7c890`)
- [x] `7.2.3.2` — material/operation line tables + schemas (same migration)
- [x] `7.2.3.3` — create/refresh from TC + sewing ledger
- [x] `7.2.3.4` — approve validation (terminal TCs)
- [x] `7.2.3.5` — plan+fact blocks on card + batch host link
- [x] `7.2.3.6` — immutability after approve
