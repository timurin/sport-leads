/**
 * @deprecated Use `DemoCreateDrawer` (CreateDrawer overlay) for demo create stubs.
 * Centered create modals are forbidden for entity create (ADR-013).
 * Kept temporarily so accidental imports fail the build loudly after removal of export usage.
 */
export { DemoCreateDrawer as DemoActionDialog } from "@/components/ui/demo-create-drawer";
